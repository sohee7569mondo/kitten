# 스텔라사주 — 구조서 vs 현재 사이트

사이트: `stellasaju.com` (blog_id 256378924, Atomic/Business, twentytwentythree)

## 지금 깔려 있는 것

| 플러그인 | 상태 | 쓸모 |
|---|---|---|
| **WPCode Lite** | 활성 | **PHP 스니펫·숏코드를 넣을 수 있음.** 뒤쪽 기능의 실행 경로 |
| Jetpack / Akismet / Page Optimize | 활성 | 기본 |
| MailPoet + Premium | 활성 | 메일 발송 (가입 환영메일·소멸 알림에 재활용 가능) |
| Gutenberg | 활성 | 블록 에디터 최신판 |
| Classic Editor | 비활성 | — |

**없는 것**: 회원/멤버십 플러그인, WooCommerce, 결제 게이트웨이, 폼 플러그인, 커스텀 필드 플러그인.

---

## 구조서 → 구현 난이도

### ✅ 1. 홈 — **완료** (`wordpress/home-page-62.html`, v7)
구조서 그대로 히어로 → 무료운세 4개 → 여섯 개의 문 → 스타운세 배너 → 철학 30/50/20 → 클로징 CTA.
순수 HTML/CSS라 붙여넣기만 하면 끝.

### 🟡 2. 스타운세 페이지 — **결제·로그인과 무관해서 다음에 만들기 제일 좋음**
| 구조서 항목 | 구현 |
|---|---|
| 인물 DB | 커스텀 포스트 타입 `star` (WPCode PHP 스니펫으로 등록) |
| 검토 대기 / 공개 | 워드프레스 기본 상태 그대로 — `pending`=검토대기, `publish`=공개, `draft`=반려 |
| 관리자 승인/수정/반려 | **wp-admin 글목록이 그대로 관리화면이 됨.** 따로 만들 필요 없음 |
| 조회된 인물 목록 | `publish`만 뽑는 숏코드 |
| 이름 검색 | 있으면 즉시 표시 / 없으면 `pending`으로 생성 후 "검토 중" 안내 |
| 큐레이션 카드 · 9단계 지침 | 승인 절차에서 사람이 거르는 구조라 자동화 불필요 |
| 공통 고지문 | 템플릿에 고정 |

→ **PHP 스니펫 1개 + 숏코드 2개 + 페이지 1개.** 결제 없이 독립적으로 완성 가능.

### 🔴 3. 회원가입/로그인 — 뒤쪽 전부의 전제조건
- 워드프레스 기본 유저에 **커스텀 필드**(생년월일시·출생지·개인정보 동의·추천인 코드)를 붙임 → user meta
- 기본 로그인 화면은 워드프레스 로고가 박혀 있어서 **커스텀 가입/로그인 페이지**가 필요
- 추천인 코드: 가입 시 발급 + 입력 시 양쪽에 보너스 구슬

### 🔴 4. 구슬(크레딧) 시스템 — 가디언 상세페이지의 전제조건
- 유료 구슬 / 보너스 구슬을 **따로** 기록해야 소멸 처리가 됨 → 잔액 하나로는 불가능, **원장(ledger) 테이블** 필요
- 차감 순서(보너스 먼저? 만료 임박 먼저?)와 소멸 기한을 정해야 함
- 소멸 예정 알림 → MailPoet 재활용

### 🔴 5. 가디언 상세 페이지 6개 — 3·4번 위에 올라감
공통 템플릿 1개 + 가디언별 데이터(특화 질문 목록·가격). 로그인 분기, 폼 저장, 구슬 차감이 전부 PHP.

### ⚠️ 6. 결제 — **가장 큰 리스크**
WordPress.com Atomic에 WooCommerce는 설치 가능하지만, **한국 PG(토스페이먼츠·이니시스·나이스페이) 연동이 관건**입니다.
여기가 전체 일정의 병목이라 방식을 먼저 정해야 합니다.

### 🔴 7. 마이페이지 (일반/관리자)
일반: 구슬 잔액·소멸 예정·추천인 코드·상담 히스토리 → 4번이 끝나야 만들 수 있음
관리자: **2번의 wp-admin 글목록으로 대부분 대체됨.** 전용 화면은 나중에 필요하면.

---

## 권장 순서

```
1. 홈 배포          ← 지금 바로 (코드 완성)
2. 스타운세 페이지   ← 독립적, 결제 없이 완성 가능. 사이트가 "돌아가는" 느낌을 줌
3. 회원가입/로그인 + 프로필 필드 + 추천인 코드
4. 구슬 원장 (충전 없이 수동 지급부터)
5. 가디언 상세 6개  ← 3·4 위에 올라감
6. 결제 연동        ← 방식 확정 후
7. 마이페이지
```

2번까지만 해도 **공개 가능한 사이트**가 됩니다. 3번부터는 회원 데이터를 다루니
개인정보 처리방침 페이지도 같이 준비해야 합니다 (생년월일시·출생지는 민감정보에 가깝습니다).

---

## 정해야 할 것

1. **띠별운세(`/zodiac-year/`)·사주총평(`/weekly-summary/`)** — 구조서에 없는데 페이지는 살아 있습니다.
   현재 홈에서 링크가 빠져 있어 들어갈 길이 없습니다. 스타운세 페이지에 넣을지, 무료운세로 올릴지, 접을지.
2. **결제 방식** — 위 6번.
3. **구슬 정책** — 유료/보너스 차감 순서, 소멸 기한, 가디언별 가격.
