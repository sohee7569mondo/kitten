/* ════════════════════════════════════════════════════════════
   근거 쪽 글을 소희 님이 고쳐 쓰신 것으로 바꿉니다

   두 쪽, 여섯 자리입니다.

   대운 (근거 넷) — 마무리 세 문단
     「대운은 운이 좋아지고 나빠지는 표가 아닙니다」 뒤에 한 문단이
     더 붙습니다. 어떤 시기에는 가진 것을 키우고 어떤 시기에는 새
     판으로 옮겨간다는 이야기, 그리고 「좋은 운인가」보다 「지금
     어떤 판에 올라와 있는가」를 먼저 보라는 말입니다. 표를 보고
     좋고 나쁨을 세던 손님이 자리를 세게 됩니다.

   별자리 맛보기 — 다섯 자리
     ② 「그런데 속으로 당신은」 → 「그런데 조금 더 들여다보면」.
        「다릅니다」도 「다를 수 있습니다」로 눅였습니다. 별자리는
        단정하는 쪽이 아니니까요.
     ③ 천정을 설명하는 순서를 뒤집습니다. 지금은 해석을 먼저 던지고
        천정이 뭔지를 뒤에 붙이는데, 손님은 천정이 뭔지 모르는 채로
        해석부터 읽고 있었습니다. 자리를 먼저 알려주고 해석으로
        갑니다. 「천정(MC)」로 영문도 같이 적었습니다.
     ④ 겹침으로 넘어가는 다리를 새로 놓습니다. 「사주가 흙에 발을
        딛고 읽는 언어라면, 별자리는 하늘의 움직임을 읽는 언어입니다」
        — 홈 첫 줄과 같은 말이라 책과 홈이 이어집니다.
     ⑤ 겹치는 자리를 별자리 쪽부터 말하고 사주를 더합니다. 지금은
        사주부터 말해서, 별자리 쪽인데 사주가 앞에 나왔습니다.
     ⑥ 상승궁이 여덟 글자와 다를 때 — 상승궁이 무엇인지 한 줄을
        넣고, 두 문단으로 나눴습니다. 「쉬실 때는 이 얼굴부터
        내려놓으세요」가 「쉬는 시간만큼은 굳이 단단한 모습을
        유지하지 않아도 됩니다」로 부드러워집니다.

   글자 하나 — 소희 님이 작은따옴표로 쓰신 자리는 책이 쓰는 「」로
   옮겼습니다. 책 전체가 「」라서요. 그대로 두는 게 나으시면
   말씀만 주세요.

   ★ 이 스니펫은 patch160_taste 가 이미 들어간 상태를 고칩니다.
     맛보기 패치를 먼저 넣으세요.

   페이지 내용을 wp_update_post 로 저장하면 워드프레스가 <script> 를
   지워버립니다. 그래서 데이터베이스에 바로 씁니다.

   붙여넣기 : WPCode → 새 스니펫 → PHP Snippet → 저장 → Active
   ★ 위치(Location)를 반드시 「어디서나 실행 / Run Everywhere」로 바꾸세요.

   ① 먼저 보기   https://stellasaju.com/?stella_ev=1
   ② 정말 바꾸기 https://stellasaju.com/?stella_ev=apply
   ③ 되돌리기    https://stellasaju.com/?stella_ev=undo
   ════════════════════════════════════════════════════════════ */

add_action( 'template_redirect', function () {

	if ( ! isset( $_GET['stella_ev'] ) ) { return; }
	if ( ! current_user_can( 'manage_options' ) ) { return; }

	global $wpdb;

	$PAGE = 160;
	$META = '_stella_ev_backup';
	$mode = sanitize_key( wp_unslash( $_GET['stella_ev'] ) );

	header( 'Content-Type: text/html; charset=utf-8' );
	echo '<meta charset="utf-8"><style>body{background:#12101B;color:#F2ECE0;'
	   . 'font-family:-apple-system,"Apple SD Gothic Neo","Malgun Gothic",sans-serif;'
	   . 'line-height:1.8;padding:34px 24px;max-width:820px;margin:0 auto}'
	   . 'h1{color:#D4AF6A;font-size:1.4rem;margin:0 0 18px}h2{color:#D4AF6A;font-size:1rem;margin:22px 0 6px}'
	   . 'b{color:#F0D9A8}.ok{color:#9AD1A5}.no{color:#E5A0A0}'
	   . 'a{color:#D4AF6A}code{background:rgba(212,175,106,.12);padding:2px 6px;border-radius:3px}</style>';

	$row = $wpdb->get_row( $wpdb->prepare(
		"SELECT ID, post_content FROM {$wpdb->posts} WHERE ID = %d", $PAGE ) );
	if ( ! $row ) { echo '<h1>160쪽을 못 찾았습니다.</h1>'; exit; }

	if ( 'undo' === $mode ) {
		$old = get_post_meta( $PAGE, $META, true );
		echo '<h1>되돌리기</h1>';
		if ( ! $old ) { echo '<p>되돌릴 것이 없습니다.</p>'; exit; }
		$wpdb->update( $wpdb->posts, array( 'post_content' => $old ), array( 'ID' => $PAGE ) );
		clean_post_cache( $PAGE );
		delete_post_meta( $PAGE, $META );
		echo '<p class="ok">' . number_format( strlen( $old ) ) . ' 바이트로 되돌렸습니다.</p>';
		echo '<p><a href="/reading-book/?demo=door-love-2">책 보기</a></p>';
		exit;
	}

	$c = $row->post_content;
	$before = strlen( $c );

	$jobs = array();

	$jobs[] = array(
		'name' => '① 대운 · 마무리 세 문단',
		'find' => '/\'<p>지금은\\s+<em>\'\\+lk\\.list\\[nowIdx\\]\\.han\\+\'<\/em>\\s+대운\\s+한가운데입니다\\.\\s+\'\\+\\s+\'앞으로\\s+\'\\+Math\\.max\\(0,\\s+Math\\.round\\(lk\\.list\\[nowIdx\\]\\.to\\-age\\)\\)\\+\'년쯤\\s+이\\s+판이\\s+이어지고,\\s+\'\\+\\s+\'그다음\\s+<em>\'\\+\\(lk\\.list\\[nowIdx\\+1\\]\\?lk\\.list\\[nowIdx\\+1\\]\\.han:\'다음\'\\)\\+\'<\/em>\\s+대운으로\\s+넘어갑니다\\.<\/p>\'\\s+:\\s+\'<p>대운이\\s+시작되기\\s+전이거나\\s+표의\\s+범위를\\s+넘어선\\s+나이입니다\\.<\/p>\'\\)\\+\\s+\'<p>대운은\\s+운이\\s+좋아지고\\s+나빠지는\\s+표가\\s+아닙니다\\.\\s+<em>판이\\s+바뀌는\\s+시점<\/em>의\\s+표입니다\\.\\s+\'\\+\\s+\'같은\\s+사람이\\s+같은\\s+일을\\s+해도,\\s+판이\\s+바뀌면\\s+결과가\\s+달라집니다\\.<\/p>\'\\);/u',
		'to'   => "    '<p>지금은 <em>'+lk.list[nowIdx].han+'</em> 대운 한가운데에 있습니다. '+\n      '앞으로 '+Math.max(0, Math.round(lk.list[nowIdx].to-age))+'년 정도 이 판이 이어지고, '+\n      '그다음에는 <em>'+(lk.list[nowIdx+1]?lk.list[nowIdx+1].han:'다음')+'</em> 대운으로 넘어갑니다.</p>'\n      : '<p>대운이 시작되기 전이거나 표의 범위를 넘어선 나이입니다.</p>')+\n    '<p>대운은 운이 좋고 나쁜지를 표시해 놓은 표가 아닙니다. '+\n    '삶의 <em>판</em>이 바뀌는 시점을 보여주는 것입니다.</p>'+\n    '<p>같은 사람이 같은 일을 해도 판이 달라지면 결과가 달라집니다. '+\n    '어떤 시기에는 내가 가진 것을 키우는 일이 중요하고, '+\n    '어떤 시기에는 새로운 판으로 옮겨가는 것이 중요합니다. '+\n    '그래서 대운을 볼 때는 「좋은 운인가, 나쁜 운인가」보다 '+\n    '<em>「지금 어떤 판에 올라와 있는가」</em>를 먼저 봐야 합니다.</p>');\n",
	);

	$jobs[] = array(
		'name' => '② 하늘 · 첫인상과 속',
		'find' => '/\'<p>\'\\+it\\.asc\\+\'\\.\\s+그런데\\s+속으로\\s+당신은\\s+<em>\'\\+it\\.sun\\+\'<\/em>입니다\\.\'\\+\\s+\\(st\\.sun\\.name===st\\.ascendant\\.name\\s+\\?\\s+\'\\s+태양과\\s+상승궁이\\s+같은\\s+\'\\+st\\.sun\\.name\\+\'라,\\s+보이는\\s+모습과\\s+속이\\s+거의\\s+같습니다\\.\\s+\'\\+\\s+\'숨기는\\s+것이\\s+없는\\s+대신\\s+숨을\\s+곳도\\s+없어요\\.\'\\s+:\\s+\'\\s+태양은\\s+\'\\+st\\.sun\\.name\\+\'인데\\s+상승궁은\\s+\'\\+st\\.ascendant\\.name\\+\'라,\\s+처음\\s+본\\s+사람이\\s+아는\\s+모습과\\s+\'\\+\\s+\'오래\\s+본\\s+사람이\\s+아는\\s+모습이\\s+다릅니다\\.\\s+설명을\\s+두\\s+번\\s+해야\\s+하는\\s+자리가\\s+자주\\s+생기고,\\s+\'\\+\\s+\'그게\\s+은근히\\s+지칩니다\\.\'\\)\\+\'<\/p>\'\\+/u',
		'to'   => "    '<p>'+it.asc+'. 그런데 조금 더 들여다보면 당신은 <em>'+it.sun+'</em>입니다.'+\n    (st.sun.name===st.ascendant.name\n      ? ' 태양과 상승궁이 같은 '+st.sun.name+'라, 보이는 모습과 속이 거의 같습니다. '+\n        '숨기는 것이 없는 대신 숨을 곳도 없어요.'\n      : ' 태양은 '+st.sun.name+'인데 상승궁은 '+st.ascendant.name+'라, 처음 만난 사람이 보는 당신과 '+\n        '오래 지켜본 사람이 아는 당신이 다를 수 있습니다. 그래서 나를 설명하는 데 '+\n        '한 번 더 말해야 하는 일이 생기고, 그게 은근히 지칩니다.')+'</p>'+\n",
	);

	$jobs[] = array(
		'name' => '③ 하늘 · 천정이 무엇인지 먼저',
		'find' => '/\'<p>밖에서\\s+—\\s+그러니까\\s+일을\\s+하고\\s+이름이\\s+알려지는\\s+쪽에서\\s+당신이\\s+맡게\\s+되는\\s+것은\\s+\'\\+\\s+\'<em>\'\\+it\\.mc\\+\'<\/em>입니다\\.\\s+\'\\+\\s+\\(it\\.mc2\\s+\\?\\s+it\\.mc2\\+\'\\.\\s+\'\\s+:\\s+\'\'\\)\\+\\s+\'천정은\\s+태어나던\\s+순간\\s+머리\\s+꼭대기에\\s+떠\\s+있던\\s+별자리인데,\\s+\'\\+\\s+\'남들\\s+눈에\\s+보이는\\s+내\\s+자리를\\s+가리킵니다\\.<\/p>\'\\+/u',
		'to'   => "    '<p>일을 하고 이름을 알리는 자리, 다시 말해 <em>세상에 나를 드러내는 자리</em>에서 '+\n    '당신이 어떤 모습이 되는지를 봅니다.</p>'+\n    '<p>천정(MC)은 태어난 순간 하늘의 가장 높은 곳에 있던 지점으로, '+\n    '사회적으로 어떤 모습으로 드러나고 어떤 자리를 향해 가는지를 봅니다. '+\n    '그래서 천정 '+st.midheaven.name+'는 <em>'+it.mc+'</em>'+\n    (R.hasBatchim(it.mc)?'이라는':'라는')+' 해석으로 이어집니다.'+\n    (it.mc2 ? ' '+it.mc2+'.' : '')+'</p>'+\n",
	);

	$jobs[] = array(
		'name' => '④ 하늘 · 두 언어를 잇는 다리',
		'find' => '/out=\'<p>사주가\\s+흙의\\s+언어라면\\s+별자리는\\s+빛의\\s+언어입니다\\.\\s+\'\\+\\s+\'두\\s+언어가\\s+같은\\s+곳을\\s+가리키면,\\s+그\\s+자리는\\s+웬만해선\\s+틀리지\\s+않아요\\.\\s+\'\\+\\s+\'당신은\\s+이렇게\\s+겹칩니다\\.<\/p>\';\\s+if\\(hit\\.length===0\\)\\{\\s+return\\s+out\\+\\s+\'<p>세\\s+자리\\s+모두\\s+여덟\\s+글자와\\s+겹치는\\s+데가\\s+없습니다\\.\\s+드문\\s+쪽이에요\\.\\s+\'\\+\\s+\'폭이\\s+넓다는\\s+뜻이고,\\s+그만큼\\s+당신을\\s+한마디로\\s+설명하기가\\s+어렵습니다\\.\\s+\'\\+\\s+\'사람마다\\s+당신을\\s+다르게\\s+기억하는\\s+이유가\\s+여기\\s+있어요\\.\\s+\'\\+\\s+\'이런\\s+분은\\s+남이\\s+붙여주는\\s+이름을\\s+기다리지\\s+마시고,\\s+\'\\+\\s+\'<em>당신이\\s+먼저\\s+자기를\\s+어떻게\\s+부를지\\s+정하는\\s+편<\/em>이\\s+낫습니다\\.<\/p>\';\\s+\\}/u',
		'to'   => "      out='<p>사주가 흙에 발을 딛고 읽는 언어라면, 별자리는 하늘의 움직임을 읽는 언어입니다. '+\n          '서로 다른 체계이지만 같은 성향을 가리킬 때가 있습니다. '+\n          '그럴 때 저는 <em>그 겹치는 부분</em>을 중요하게 봅니다.</p>';\n\n      if(hit.length===0){\n        return out+\n          '<p>그런데 세 자리 모두 여덟 글자와 겹치는 데가 없습니다. 드문 쪽이에요. '+\n          '폭이 넓다는 뜻이고, 그만큼 당신을 한마디로 설명하기가 어렵습니다. '+\n          '사람마다 당신을 다르게 기억하는 이유가 여기 있어요. '+\n          '이런 분은 남이 붙여주는 이름을 기다리지 마시고, '+\n          '<em>당신이 먼저 자기를 어떻게 부를지 정하는 편</em>이 낫습니다.</p>';\n      }\n\n      out+='<p>당신에게는 그런 겹침이 분명하게 보입니다.</p>';\n",
	);

	$jobs[] = array(
		'name' => '⑤ 하늘 · 겹치는 자리',
		'find' => '/out\\+=\'<p>·\\s+<em>\'\\+nm\\.join\\(\'\\s+·\\s+\'\\)\\+\'<\/em>\\s+—\\s+여덟\\s+글자에서도\\s+\'\\+\\s+keys\\[i\\]\\+\'이\\s+\'\\+NUM\\[lot\\[0\\]\\.n\\]\\+\'\\s+자리입니다\\.\\s+\'\\+\\s+\\(TS\\[keys\\[i\\]\\]\\|\\|\'같은\\s+힘\'\\)\\+\'이\\s+\'\\+\\s+\'두\\s+언어에서\\s+같이\\s+나옵니다\\.\'\\+\\s+\\(lot\\.length>1\\s+\\?\\s+\'\\s+이렇게\\s+두\\s+자리가\\s+한꺼번에\\s+겹치면,\\s+이건\\s+당신을\\s+설명하는\\s+첫\\s+문장이\\s+됩니다\\.\'\\s+:\\s+\'\'\\)\\+\\s+\'<\/p>\';/u',
		'to'   => "        var say=TS[keys[i]]||'같은 힘';\n        out+='<p>· <em>'+nm.join(' · ')+'</em> — '+(lot.length>1?NUM[lot.length]+' 곳 모두 ':'')+\n             say+(R.hasBatchim(say)?'을':'를')+' 보여줍니다. '+\n             '여기에 사주의 '+keys[i]+'이 '+NUM[lot[0].n]+' 자리 더해집니다. '+\n             '서로 다른 언어가 같은 방향을 가리키고 있습니다.'+\n             (lot.length>1 ? ' 이런 겹침은 당신을 설명하는 <em>중요한 첫 문장</em>이 됩니다.' : '')+\n             '</p>';\n",
	);

	$jobs[] = array(
		'name' => '⑥ 하늘 · 상승궁이 다를 때',
		'find' => '/\'상승궁\':\'는\\s+여덟\\s+글자와\\s+다른\\s+말을\\s+합니다\\.\\s+타고난\\s+쪽이\\s+아니라\\s+\'\\+\\s+\'<em>살면서\\s+당신이\\s+만들어\\s+붙인\\s+얼굴<\/em>이에요\\.\\s+익힌\\s+것이라\\s+잘\\s+쓰시는데,\\s+\'\\+\\s+\'쓰고\\s+나면\\s+유난히\\s+피곤하십니다\\.\\s+쉬실\\s+때는\\s+이\\s+얼굴부터\\s+내려놓으세요\\.\',/u',
		'to'   => "          '상승궁':'는 조금 다른 이야기를 합니다. 상승궁은 첫인상이나 세상에 나를 '+\n                   '드러내는 방식을 보여주는 자리예요. 속으로 가진 모습과 겉으로 '+\n                   '보여주는 모습 사이에 차이가 있다는 뜻입니다. 타고난 쪽이 아니라 '+\n                   '<em>살아오면서 더 강하게 써온 얼굴</em>에 가까워요.</p>'+\n                   '<p>잘 쓰는 얼굴이지만, 오래 쓰고 있으면 피곤합니다. '+\n                   '그래서 쉬는 시간만큼은 굳이 단단한 모습을 유지하지 않아도 됩니다.',\n",
	);

	$apply = ( 'apply' === $mode );
	echo '<h1>' . ( $apply ? '바꿨습니다' : '바꾸면 이렇게 됩니다 (아직 안 바꿨습니다)' ) . '</h1>';

	$ok = true;
	foreach ( $jobs as $j ) {
		$n = preg_match_all( $j['find'], $c );
		echo '<h2>' . esc_html( $j['name'] ) . '</h2>';
		if ( 1 === $n ) {
			echo '<p class="ok">찾았습니다 — 한 군데.</p>';
		} elseif ( 0 === $n ) {
			echo '<p class="no">못 찾았습니다. 이미 바꿨거나 코드가 달라졌습니다.</p>';
			$ok = false;
		} else {
			echo '<p class="no">' . intval( $n ) . '군데가 걸립니다. 한 군데여야 안전합니다.</p>';
			$ok = false;
		}
	}

	if ( ! $ok ) {
		echo '<p class="no">한 자리가 한 군데씩 걸릴 때만 바꿉니다. 아무것도 건드리지 않았습니다.</p>';
		exit;
	}

	$new = $c;
	foreach ( $jobs as $j ) {
		$new = preg_replace( $j['find'], str_replace( '\\', '\\\\', $j['to'] ), $new, 1 );
		if ( null === $new ) { echo '<p class="no">바꾸다가 멈췄습니다. 아무것도 저장하지 않았습니다.</p>'; exit; }
	}

	echo '<p>' . number_format( $before ) . ' → ' . number_format( strlen( $new ) ) . ' 바이트</p>';

	if ( ! $apply ) {
		echo '<p>정말 바꾸시려면 <code>/?stella_ev=apply</code> 를 여세요.</p>';
		exit;
	}

	if ( ! get_post_meta( $PAGE, $META, true ) ) {
		update_post_meta( $PAGE, $META, $c );
	}
	$wpdb->update( $wpdb->posts, array( 'post_content' => $new ), array( 'ID' => $PAGE ) );
	clean_post_cache( $PAGE );

	echo '<p class="ok">바꿨습니다.</p>';
	echo '<p>되돌리려면 <code>/?stella_ev=undo</code></p>';
	echo '<p><a href="/reading-book/?demo=door-love-2">책 보기</a></p>';
	exit;
} );
