/* ══════════════════════════════════════════════════════════
   지금 책에 무엇이 들어 있나 물어보기 · 소희 님께   2026-09-11

   제가 가진 책 사본이 낡아서, 고칠 자리를 못 찾는 일이 생깁니다.
   이 스니펫은 아무것도 바꾸지 않습니다. 그냥 보여주기만 합니다.

   쓰는 법
     WPCode → 새 스니펫 → PHP Snippet → 「어디서나 실행」 → 저장 → 활성
     그리고 주소창에
        https://stellasaju.com/?stella_probe=1
     나온 글을 통째로 저에게 붙여주시면 됩니다.

   ★ 이 스니펫은 읽기만 합니다. 되돌릴 것도 없습니다.
   ══════════════════════════════════════════════════════════ */

add_action( 'template_redirect', function () {

	if ( ! isset( $_GET['stella_probe'] ) ) { return; }
	if ( ! current_user_can( 'manage_options' ) ) { return; }

	global $wpdb;
	header( 'Content-Type: text/plain; charset=utf-8' );

	$page = get_page_by_path( 'reading-book' );
	if ( ! $page ) { echo '책 쪽이 없습니다.'; exit; }
	$id = (int) $page->ID;
	$c  = (string) $wpdb->get_var( $wpdb->prepare(
		"SELECT post_content FROM {$wpdb->posts} WHERE ID = %d", $id ) );

	echo "═══ 지금 책에 무엇이 있나 ═══\n";
	echo '쪽 ' . $id . ' · ' . number_format( strlen( $c ) ) . "글자\n\n";

	/* 찾아볼 조각들 — 이름, 찾을 글자 */
	$Q = array(
		array( '09 옛 제목',   '<h2>당신이 아무 말도 하기 전에</h2>' ),
		array( '09 새 제목?',  '님의 결</h2>' ),
		array( '09 사주에서',  '사주에서 보는 ' ),
		array( '09 접기옛',    'var SIP=\'당신이 아무 말도 하기 전에\';' ),
		array( '09 접기새?',   'var SIP=\'사주에서 보는 \';' ),
		array( '09 접기아무',  'var SIP=' ),
		array( '13 옛 제목',   '맞지 않은 부분을 정리해보았습니다' ),
		array( '13 타고난',    '타고난 성향과 적어주신' ),
		array( '13 어긋난',    '어긋난 자리</h2>' ),
		array( '18 되풀이A',   'var h = \'되풀이되는 자리\';' ),
		array( '18 되풀이B',   'var h=\'되풀이되는 자리\';' ),
		array( '18 되풀이C',   '되풀이되는 자리\'' ),
		array( '확인 · 벼리',  '인연을 잇는 벼리' ),
		array( '확인 · 저울',  '글자 속에 숨은 글자까지' ),
		array( '확인 · 그리면', '사주로 당신을 그리면' ),
	);

	foreach ( $Q as $q ) {
		list( $nm, $needle ) = $q;
		$n = substr_count( $c, $needle );
		echo sprintf( "%-13s %d군데", $nm, $n );
		if ( $n > 0 ) {
			$p = strpos( $c, $needle );
			$s = max( 0, $p - 70 );
			$cut = substr( $c, $s, strlen( $needle ) + 140 );
			$cut = preg_replace( '/\s+/u', ' ', $cut );
			echo "\n    …" . $cut . "…";
		}
		echo "\n";
	}

	echo "\n═══ 여기까지 · 위를 통째로 붙여주세요 ═══";
	exit;
}, 1 );
