/* ════════════════════════════════════════════════════════════
   유앤미 ⑦ · 스레드·X·페이스북 카드 (og 태그)

   유앤미 결과 링크를 SNS 에 붙이면 점수 카드가 보이게 합니다.
   ④번에 얹었다가 화면이 통째로 가려진 일이 있어 따로 뗐습니다.

   하는 일은 하나뿐입니다 — 머리말에 og 태그 넣기.
   워드프레스의 페이지 찾기에는 손대지 않습니다.

   붙여넣기 : WPCode → 새 스니펫 → PHP Snippet →
              Auto Insert → Run Everywhere → 저장 → Active

   확인 : 유앤미에서 궁합 결과를 낸 뒤 그 주소를 스레드에 붙여보기
   ════════════════════════════════════════════════════════════ */

add_action( 'wp_head', function () {

	/* 유앤미 화면이고, 점수가 실려 있을 때만 */
	$uri = isset( $_SERVER['REQUEST_URI'] ) ? $_SERVER['REQUEST_URI'] : '';
	if ( false === strpos( $uri, '/uandme' ) ) { return; }
	if ( ! isset( $_GET['sc'] ) ) { return; }

	$score = max( 0, min( 99, intval( $_GET['sc'] ) ) );
	$a     = isset( $_GET['a'] ) ? max( 0, min( 40, intval( $_GET['a'] ) ) ) : 0;
	$b     = isset( $_GET['b'] ) ? max( 0, min( 30, intval( $_GET['b'] ) ) ) : 0;
	$c     = isset( $_GET['c'] ) ? max( 0, min( 30, intval( $_GET['c'] ) ) ) : 0;
	$rel   = isset( $_GET['rel'] ) ? preg_replace( '/[^a-z_]/', '', (string) $_GET['rel'] ) : 'lover';
	if ( ! $rel ) { $rel = 'lover'; }

	if ( $score >= 95 )      { $tier = '천생연분'; }
	else if ( $score >= 90 ) { $tier = '완전 찰떡궁합'; }
	else if ( $score >= 80 ) { $tier = '환상의 케미'; }
	else if ( $score >= 70 ) { $tier = '좋은 궁합'; }
	else if ( $score >= 60 ) { $tier = '무난한 사이'; }
	else if ( $score >= 50 ) { $tier = '노력형 궁합'; }
	else if ( $score >= 40 ) { $tier = '위태위태'; }
	else if ( $score >= 30 ) { $tier = '친구가 나을지도'; }
	else if ( $score >= 20 ) { $tier = '재고요망'; }
	else                     { $tier = '이번 생은 예능감'; }

	$img   = home_url( '/um-card/' . $score . '-' . $rel . '-' . $a . '-' . $b . '-' . $c );
	$title = '우리 ' . $score . '점 - ' . $tier;
	$desc  = '사주 ' . $a . '/40, 별자리 ' . $b . '/30, 띠 ' . $c . '/30. 생일만 넣으면 바로 나와요';

	$host = isset( $_SERVER['HTTP_HOST'] ) ? $_SERVER['HTTP_HOST'] : 'stellasaju.com';
	$here = ( is_ssl() ? 'https://' : 'http://' ) . $host . $uri;

	echo "\n";
	echo '<meta property="og:type" content="website">' . "\n";
	echo '<meta property="og:title" content="' . esc_attr( $title ) . '">' . "\n";
	echo '<meta property="og:description" content="' . esc_attr( $desc ) . '">' . "\n";
	echo '<meta property="og:image" content="' . esc_url( $img ) . '">' . "\n";
	echo '<meta property="og:image:width" content="800">' . "\n";
	echo '<meta property="og:image:height" content="400">' . "\n";
	echo '<meta property="og:url" content="' . esc_url( $here ) . '">' . "\n";
	echo '<meta name="twitter:card" content="summary_large_image">' . "\n";
	echo '<meta name="twitter:title" content="' . esc_attr( $title ) . '">' . "\n";
	echo '<meta name="twitter:description" content="' . esc_attr( $desc ) . '">' . "\n";
	echo '<meta name="twitter:image" content="' . esc_url( $img ) . '">' . "\n";

}, 1 );

/* 젯팩이 스스로 내보내는 og 를 우리 것으로 바꿔 끼웁니다.
   그러지 않으면 회색 스텔라사주 그림이 함께 나가 그쪽이 뽑힙니다. */
add_filter( 'jetpack_open_graph_tags', function ( $tags ) {

	$uri = isset( $_SERVER['REQUEST_URI'] ) ? $_SERVER['REQUEST_URI'] : '';
	if ( false === strpos( $uri, '/uandme' ) ) { return $tags; }
	if ( ! isset( $_GET['sc'] ) ) { return $tags; }

	$score = max( 0, min( 99, intval( $_GET['sc'] ) ) );
	$a     = isset( $_GET['a'] ) ? max( 0, min( 40, intval( $_GET['a'] ) ) ) : 0;
	$b     = isset( $_GET['b'] ) ? max( 0, min( 30, intval( $_GET['b'] ) ) ) : 0;
	$c     = isset( $_GET['c'] ) ? max( 0, min( 30, intval( $_GET['c'] ) ) ) : 0;
	$rel   = isset( $_GET['rel'] ) ? preg_replace( '/[^a-z_]/', '', (string) $_GET['rel'] ) : 'lover';
	if ( ! $rel ) { $rel = 'lover'; }

	$img = home_url( '/um-card/' . $score . '-' . $rel . '-' . $a . '-' . $b . '-' . $c );

	$tags['og:image']        = $img;
	$tags['og:image:width']  = 800;
	$tags['og:image:height'] = 400;
	$tags['twitter:card']    = 'summary_large_image';
	$tags['twitter:image']   = $img;

	return $tags;
}, 20 );
