<?php
/**
 * STELLA SAJU — 페이지 160 · 셋을 겹쳐 놓으면        PATCH: COMBO-1
 * ------------------------------------------------------------------
 * 한 번만 돌리는 스니펫입니다. 돌린 뒤에는 반드시 지우세요.
 *
 * [무엇을 고치나]  열일곱 자리를 고칩니다.
 *
 *   1) 저울 소제목과 본문의 낱말을 맞춥니다
 *      소제목은 「다투고 나서, 삭이는가 붙잡는가」로 갈렸는데 본문은
 *      「사주는 함께 쪽으로…」처럼 옛 낱말을 그대로 썼습니다.
 *      이제 본문도 「사주는 붙잡는 쪽으로…」로 나갑니다.
 *      연성의 신·직성의 신 두 문만 표에 있고, 나머지 네 문은 손대지 않습니다.
 *
 *   2) 오행을 화면에 보일 때만 풀어 씁니다 — 목 → 목(나무 목)
 *      값은 절대 안 바꿉니다. 엔진이 오행을 셀 때 쓰는 열쇠말이라
 *      바꾸면 계산이 통째로 깨집니다. 보여주는 자리 아홉 곳만 감쌉니다.
 *      뒤에 붙는 조사(이에요/예요)는 원래 글자로 고르므로 그대로 맞습니다.
 *
 *   3) 「남은 저울 셋」 → 「타고난 성향과 실제 생활이 다른 부분 — 남은 셋」
 *
 *   4) 「올해 인연의 흐름」 표 밑에 달 이름 다섯 개의 뜻을 붙입니다
 *      말이 나가는 달 · 견주는 달 · 끌리는 달 · 흔들리는 달 · 기대고 싶은 달
 *
 *   5) 연성의 신 닫는 말을 쉽고 충분하게 다시 씁니다
 *
 * [미리 알아두실 것]
 *   · 1)번과 2)번은 여섯 문 전부가 지나가는 자리를 건드립니다.
 *     표에 없는 문은 원래 낱말 그대로 나가도록 해두었지만,
 *     제가 실제로 그려본 것은 연성의 신뿐입니다.
 *   · 오행 풀어쓰기는 아홉 곳에 한꺼번에 들어갑니다. 사장님이 짚어주신
 *     「인연의 별」 자리 말고 나머지 여덟 곳은 문장 모양만 확인했습니다.
 *     한 권에서 여러 번 나오면 되풀이처럼 보일 수 있습니다.
 *   · 「남은 셋」은 사장님 말씀을 그대로 옮기지 않고 조금 고쳤습니다.
 *     앞 장 제목과 거의 같아져서 두 장이 같은 제목이 되기 때문입니다.
 *
 * 넣는 곳 : WPCode → + Add Snippet → Add Your Custom Code
 *           Code Type = PHP Snippet / Location = Run Everywhere / Active
 *           (맨 윗줄 <?php 은 빼고 붙여넣으세요)
 *
 * 돌리는 법 :
 *   미리보기 (아무것도 안 고침) : /wp-admin/?stella_patch=dry
 *   진짜로 고치기               : /wp-admin/?stella_patch=go
 *   되돌리기                    : /wp-admin/?stella_patch=undo
 *
 *   ※ 반드시 /wp-admin/ 아래 주소여야 합니다. 사이트 앞쪽 주소로는 안 뜹니다.
 *   ※ ?stella_combo= 로 치셔도 똑같이 됩니다.
 * ------------------------------------------------------------------
 */

defined( 'STELLA_C_PAGE' ) || define( 'STELLA_C_PAGE', 160 );
defined( 'STELLA_C_KEY' )  || define( 'STELLA_C_KEY',  'combo1' );

/* 다 고치고 나면 본문이 이 모양이어야 합니다 — 제가 미리 걸어보고 잰 값입니다 */
defined( 'STELLA_C_GROW' ) || define( 'STELLA_C_GROW', 15862 );
defined( 'STELLA_C_SHA' )  || define( 'STELLA_C_SHA',  'b9d58434b247eef455769e746ef91bc08a209bd9' );

$STELLA_C_EDITS = array(
	array(
		'name' => <<<'NAME0'
겹쳐 읽기 · 주제별 표 심기
NAME0,
		're'   => <<<'RE0'
~\ \ \ \ var\ majorN\ =\ 0,\ revN\ =\ 0;~s
RE0,
		'rep'  => <<<'REP0'
    /* ═══ 셋을 겹쳐 읽기 · 주제마다 ═══════════════════════════════
       2026-08-31 · 세 잣대(큰 카드 수 · 숫자 흐름 · 뒤집힘)가 주제를 안 보고
       늘 같은 글을 냈습니다. 카드가 달라도 이 장은 같아 보였어요.
       잣대 셋 × 갈래 셋 = 아홉 덩어리를 주제마다 씁니다.
       {N} 은 장수로 바뀝니다. 없는 주제는 아래 원래 글로 갑니다. */
    var COMBO_TOPIC={
      '결혼운':{
        m0:'<p>세 장이 모두 <em>작은 카드</em>입니다. 큰 카드(메이저)가 한 장도 없어요.</p>'
          +'<p>여섯 달 안에 <em>결혼 이야기가 크게 뒤집히는 일은 오지 않습니다.</em> 날짜가 갑자기 잡히거나 틀어지는 일보다, 두 분이 매일 주고받는 말이 결과를 만드는 반년이에요.</p>',
        m1:'<p>큰 카드(메이저)가 <em>한 장</em> 섞여 있습니다.</p>'
          +'<p>여섯 달 가운데 <em>한 번은 두 분 뜻과 상관없이 움직이는 일</em>이 옵니다. 집 문제일 수도, 양가 어른 이야기일 수도 있어요. 나머지는 두 분이 정하는 대로 갑니다.</p>',
        m2:'<p>큰 카드(메이저)가 <em>{N} 장</em>이나 됩니다.</p>'
          +'<p>결혼 이야기가 <em>조용히 지나가지 않는 반년</em>입니다. 좋은 쪽이든 아닌 쪽이든 지금과 같은 모양으로 끝나지는 않아요. 날짜와 절차를 너무 촘촘히 잡아두시면 오히려 어긋납니다.</p>',
        up:'<p>숫자가 <em>올라갑니다.</em> 지금이 가장 낮고 뒤로 갈수록 커지는 모양이에요.</p>'
          +'<p>지금 답답한 것이 이 반년의 바닥입니다. <em>결혼 이야기가 여기서 더 나빠지지는 않습니다.</em> 다만 올라가는 속도가 느려서 한동안은 티가 안 나요.</p>',
        down:'<p>숫자가 <em>내려갑니다.</em> 지금이 가장 크고 뒤로 갈수록 작아지는 모양이에요.</p>'
          +'<p>나빠진다는 뜻이 아닙니다. <em>벌여둔 것을 정리하고 매듭짓는 반년</em>이라는 뜻이에요. 새로 크게 벌이시면 이 여섯 달 안에는 결론이 나지 않습니다.</p>',
        flat:'<p>숫자가 <em>크게 오르지도 내리지도 않습니다.</em> 평평한 모양이에요.</p>'
          +'<p>여섯 달이 비슷하게 흘러갑니다. 답답하실 수 있는데, <em>흔들리지 않는다는 뜻</em>이기도 합니다. 이런 때에 정해둔 약속이 제일 오래 갑니다.</p>',
        r0:'<p>세 장이 모두 <em>바로 놓였습니다.</em> 뒤집힌 것이 하나도 없어요.</p>'
          +'<p>보이는 대로 가는 반년입니다. 숨은 뜻을 찾으실 것 없어요. <em>두 분 사이에 지금 오가는 말이 그대로 답</em>입니다.</p>',
        r3:'<p>세 장이 <em>모두 뒤집혀</em> 나왔습니다. 흔한 일이 아닙니다.</p>'
          +'<p>결혼 이야기가 <em>제자리에서 한 번 멈춰 있는 때</em>라는 뜻이에요. 나쁜 것이 아니라 방향을 바꾸기 직전입니다. 이럴 때 날짜부터 밀어붙이면 더 안 갑니다.</p>',
        rs:'<p>세 장 가운데 <em>{N} 장</em>이 뒤집혀 나왔습니다.</p>'
          +'<p>뒤집힌 자리는 <em>아직 때가 안 됐다</em>는 뜻입니다. 없던 일이 되는 것이 아니라 늦는 거예요. 날짜를 당기려 하시면 그 자리만 어긋납니다.</p>'
      },
      '연애운':{
        m0:'<p>세 장이 모두 <em>작은 카드</em>입니다. 큰 카드(메이저)가 한 장도 없어요.</p>'
          +'<p>여섯 달 안에 <em>관계를 통째로 바꾸는 일은 오지 않습니다.</em> 고백이나 이별 같은 큰 사건보다, 주고받는 연락과 만나는 횟수가 결과를 만드는 반년이에요.</p>',
        m1:'<p>큰 카드(메이저)가 <em>한 장</em> 섞여 있습니다.</p>'
          +'<p>여섯 달 가운데 <em>한 번은 내 뜻과 상관없이 관계가 움직이는 일</em>이 옵니다. 상대가 먼저 움직일 수도 있어요. 나머지는 당신이 정하는 대로 갑니다.</p>',
        m2:'<p>큰 카드(메이저)가 <em>{N} 장</em>이나 됩니다.</p>'
          +'<p>이 관계가 <em>조용히 지나가지 않는 반년</em>입니다. 좋은 쪽이든 아닌 쪽이든 지금과 같은 모양으로 끝나지는 않아요. 계획을 촘촘히 세우실수록 오히려 어긋납니다.</p>',
        up:'<p>숫자가 <em>올라갑니다.</em> 지금이 가장 낮고 뒤로 갈수록 커지는 모양이에요.</p>'
          +'<p>지금 애매한 것이 이 반년의 바닥입니다. <em>여기서 더 멀어지지는 않습니다.</em> 다만 가까워지는 속도가 느려서 한동안은 티가 안 나요.</p>',
        down:'<p>숫자가 <em>내려갑니다.</em> 지금이 가장 크고 뒤로 갈수록 작아지는 모양이에요.</p>'
          +'<p>식는다는 뜻만은 아닙니다. <em>들뜬 것이 가라앉고 자리를 잡는 반년</em>이기도 해요. 다만 지금 새로 시작하시면 이 여섯 달 안에는 답이 안 납니다.</p>',
        flat:'<p>숫자가 <em>크게 오르지도 내리지도 않습니다.</em> 평평한 모양이에요.</p>'
          +'<p>여섯 달이 비슷하게 흘러갑니다. 답답하실 수 있는데, <em>흔들리지 않는다는 뜻</em>이기도 합니다. 이런 때에 쌓아둔 신뢰가 제일 오래 갑니다.</p>',
        r0:'<p>세 장이 모두 <em>바로 놓였습니다.</em> 뒤집힌 것이 하나도 없어요.</p>'
          +'<p>보이는 대로 가는 반년입니다. 상대의 말에 숨은 뜻을 찾으실 것 없어요. <em>보이는 그대로</em> 읽으시면 됩니다.</p>',
        r3:'<p>세 장이 <em>모두 뒤집혀</em> 나왔습니다. 흔한 일이 아닙니다.</p>'
          +'<p>이 관계가 <em>제자리에서 한 번 멈춰 있는 때</em>라는 뜻이에요. 나쁜 것이 아니라 방향을 바꾸기 직전입니다. 이럴 때 답을 재촉하면 더 안 갑니다.</p>',
        rs:'<p>세 장 가운데 <em>{N} 장</em>이 뒤집혀 나왔습니다.</p>'
          +'<p>뒤집힌 자리는 <em>아직 다 오지 않았다</em>는 뜻입니다. 마음이 없어진 것이 아니라 늦는 거예요. 그 자리에서 서두르시면 거기만 어긋납니다.</p>'
      },
      '재회운':{
        m0:'<p>세 장이 모두 <em>작은 카드</em>입니다. 큰 카드(메이저)가 한 장도 없어요.</p>'
          +'<p>여섯 달 안에 <em>극적으로 다시 만나는 일은 오지 않습니다.</em> 어느 날 갑자기가 아니라, 작은 연락 하나가 쌓여서 길이 열리는 반년이에요.</p>',
        m1:'<p>큰 카드(메이저)가 <em>한 장</em> 섞여 있습니다.</p>'
          +'<p>여섯 달 가운데 <em>한 번은 뜻밖의 계기</em>가 옵니다. 우연히 마주치거나 소식이 들려오는 식이에요. 그 한 번을 어떻게 쓰느냐가 나머지를 정합니다.</p>',
        m2:'<p>큰 카드(메이저)가 <em>{N} 장</em>이나 됩니다.</p>'
          +'<p>이 관계가 <em>지금 자리에 그대로 머물지 않는 반년</em>입니다. 다시 이어지든 완전히 끝나든 <em>어느 쪽으로든 움직입니다.</em> 마음의 준비를 양쪽 다 해두세요.</p>',
        up:'<p>숫자가 <em>올라갑니다.</em> 지금이 가장 낮고 뒤로 갈수록 커지는 모양이에요.</p>'
          +'<p>지금이 이 반년의 바닥입니다. <em>여기서 더 멀어지지는 않습니다.</em> 다만 회복이 느려서 한동안은 아무 일도 없는 것처럼 보일 거예요.</p>',
        down:'<p>숫자가 <em>내려갑니다.</em> 지금이 가장 크고 뒤로 갈수록 작아지는 모양이에요.</p>'
          +'<p>가능성이 사라진다는 뜻은 아닙니다. <em>붙잡던 힘을 내려놓게 되는 반년</em>이라는 뜻이에요. 매달릴수록 멀어지는 자리라, 이 여섯 달은 힘을 빼시는 편이 낫습니다.</p>',
        flat:'<p>숫자가 <em>크게 오르지도 내리지도 않습니다.</em> 평평한 모양이에요.</p>'
          +'<p>여섯 달이 비슷하게 흘러갑니다. 아무 소식이 없어 답답하실 텐데, <em>끝난 것도 아니라는 뜻</em>입니다. 기다리는 동안 내 자리를 챙기세요.</p>',
        r0:'<p>세 장이 모두 <em>바로 놓였습니다.</em> 뒤집힌 것이 하나도 없어요.</p>'
          +'<p>보이는 대로 가는 반년입니다. 헤어진 이유도 지금 상황도 <em>보이는 그대로</em>예요. 숨은 사정을 찾으실 것 없습니다.</p>',
        r3:'<p>세 장이 <em>모두 뒤집혀</em> 나왔습니다. 흔한 일이 아닙니다.</p>'
          +'<p>두 사람 다 <em>제자리에서 멈춰 있는 때</em>라는 뜻이에요. 나쁜 것이 아니라 서로 정리하는 중입니다. 이럴 때 먼저 연락하시면 대개 답이 늦습니다.</p>',
        rs:'<p>세 장 가운데 <em>{N} 장</em>이 뒤집혀 나왔습니다.</p>'
          +'<p>뒤집힌 자리는 <em>그쪽이 아직 준비되지 않았다</em>는 뜻입니다. 마음이 없어진 것이 아니라 늦는 거예요. 재촉하시면 그 자리만 어긋납니다.</p>'
      },
      '이별운':{
        m0:'<p>세 장이 모두 <em>작은 카드</em>입니다. 큰 카드(메이저)가 한 장도 없어요.</p>'
          +'<p>여섯 달 안에 <em>마음이 한 번에 정리되는 일은 오지 않습니다.</em> 어느 날 갑자기 괜찮아지는 게 아니라, 하루하루가 쌓여서 나아지는 반년이에요.</p>',
        m1:'<p>큰 카드(메이저)가 <em>한 장</em> 섞여 있습니다.</p>'
          +'<p>여섯 달 가운데 <em>한 번은 마음이 크게 흔들리는 일</em>이 옵니다. 소식을 듣거나 마주치는 일일 수 있어요. 그 한 번만 넘기시면 나머지는 견딜 만합니다.</p>',
        m2:'<p>큰 카드(메이저)가 <em>{N} 장</em>이나 됩니다.</p>'
          +'<p>조용히 지나가는 반년이 아닙니다. 마음이 <em>여러 번 크게 오르내립니다.</em> 오늘 괜찮다가 내일 무너져도 이상한 일이 아니에요. 그게 회복의 모양입니다.</p>',
        up:'<p>숫자가 <em>올라갑니다.</em> 지금이 가장 낮고 뒤로 갈수록 커지는 모양이에요.</p>'
          +'<p><em>지금이 가장 아픈 때입니다.</em> 여기서 더 나빠지지는 않아요. 다만 나아지는 속도가 느려서 한동안은 나아진 게 느껴지지 않을 겁니다.</p>',
        down:'<p>숫자가 <em>내려갑니다.</em> 지금이 가장 크고 뒤로 갈수록 작아지는 모양이에요.</p>'
          +'<p>더 힘들어진다는 뜻이 아닙니다. <em>들끓던 감정이 가라앉는 반년</em>이라는 뜻이에요. 지금 크게 결정하실 일이 있다면 이 여섯 달을 넘기고 하세요.</p>',
        flat:'<p>숫자가 <em>크게 오르지도 내리지도 않습니다.</em> 평평한 모양이에요.</p>'
          +'<p>여섯 달이 비슷하게 흘러갑니다. 나아지는 것도 나빠지는 것도 없어 답답하실 텐데, <em>더는 무너지지 않는다는 뜻</em>입니다. 이런 때가 회복의 시작이에요.</p>',
        r0:'<p>세 장이 모두 <em>바로 놓였습니다.</em> 뒤집힌 것이 하나도 없어요.</p>'
          +'<p>보이는 대로 가는 반년입니다. 끝난 이유를 다시 파헤치실 것 없어요. <em>보이는 그대로</em> 받아들이시면 됩니다.</p>',
        r3:'<p>세 장이 <em>모두 뒤집혀</em> 나왔습니다. 흔한 일이 아닙니다.</p>'
          +'<p>지금 <em>제자리에서 한 번 멈춰 있는 때</em>라는 뜻이에요. 나쁜 것이 아니라 방향을 바꾸기 직전입니다. 이럴 때 억지로 잊으려 하면 더 오래 걸립니다.</p>',
        rs:'<p>세 장 가운데 <em>{N} 장</em>이 뒤집혀 나왔습니다.</p>'
          +'<p>뒤집힌 자리는 <em>아직 다 지나지 않았다</em>는 뜻입니다. 회복이 안 된 것이 아니라 늦는 거예요. 그 자리에서 서두르시면 거기만 덧납니다.</p>'
      },
      '짝사랑':{
        m0:'<p>세 장이 모두 <em>작은 카드</em>입니다. 큰 카드(메이저)가 한 장도 없어요.</p>'
          +'<p>여섯 달 안에 <em>극적인 일은 오지 않습니다.</em> 고백이나 사귐 같은 큰 사건보다, 마주치는 횟수와 나누는 말이 이 마음의 방향을 정하는 반년이에요.</p>',
        m1:'<p>큰 카드(메이저)가 <em>한 장</em> 섞여 있습니다.</p>'
          +'<p>여섯 달 가운데 <em>한 번은 뜻밖의 자리</em>가 옵니다. 둘만 남게 되거나 마음을 말할 틈이 생기는 식이에요. 그 한 번이 이 반년의 갈림길입니다.</p>',
        m2:'<p>큰 카드(메이저)가 <em>{N} 장</em>이나 됩니다.</p>'
          +'<p>이 마음이 <em>지금 자리에 그대로 머물지 않는 반년</em>입니다. 이루어지든 접게 되든 <em>어느 쪽으로든 움직입니다.</em> 오래 끌던 마음에 결말이 나는 때예요.</p>',
        up:'<p>숫자가 <em>올라갑니다.</em> 지금이 가장 낮고 뒤로 갈수록 커지는 모양이에요.</p>'
          +'<p>지금 막막한 것이 이 반년의 바닥입니다. <em>여기서 더 멀어지지는 않습니다.</em> 다만 가까워지는 속도가 느려서 한동안은 티가 안 나요.</p>',
        down:'<p>숫자가 <em>내려갑니다.</em> 지금이 가장 크고 뒤로 갈수록 작아지는 모양이에요.</p>'
          +'<p>나쁜 일이 생긴다는 뜻이 아닙니다. <em>들뜬 마음이 가라앉는 반년</em>이라는 뜻이에요. 그러면서 이 마음이 무엇이었는지 스스로 알게 되실 겁니다.</p>',
        flat:'<p>숫자가 <em>크게 오르지도 내리지도 않습니다.</em> 평평한 모양이에요.</p>'
          +'<p>여섯 달이 비슷하게 흘러갑니다. 아무 진전이 없어 답답하실 텐데, <em>끝난 것도 아니라는 뜻</em>입니다. 다만 이 상태가 길어지면 지치는 쪽은 당신이에요.</p>',
        r0:'<p>세 장이 모두 <em>바로 놓였습니다.</em> 뒤집힌 것이 하나도 없어요.</p>'
          +'<p>보이는 대로 가는 반년입니다. 그 사람의 말에 숨은 뜻을 찾으실 것 없어요. <em>보이는 그대로</em>가 지금의 답입니다.</p>',
        r3:'<p>세 장이 <em>모두 뒤집혀</em> 나왔습니다. 흔한 일이 아닙니다.</p>'
          +'<p>이 마음이 <em>제자리에서 한 번 멈춰 있는 때</em>라는 뜻이에요. 나쁜 것이 아니라 방향을 바꾸기 직전입니다. 이럴 때 마음을 전하시면 대개 답이 늦습니다.</p>',
        rs:'<p>세 장 가운데 <em>{N} 장</em>이 뒤집혀 나왔습니다.</p>'
          +'<p>뒤집힌 자리는 <em>아직 때가 오지 않았다</em>는 뜻입니다. 가망이 없는 것이 아니라 늦는 거예요. 그 자리에서 서두르시면 거기만 어긋납니다.</p>'
      }
    };
    function comboSay(key, n){
      if(slug!=='door-love'){ return ''; }
      var m=COMBO_TOPIC[topic];
      if(!m){ return ''; }
      if(!m[key]){ return ''; }
      return m[key].replace('{N}', hanNum(n));
    }

    var majorN = 0, revN = 0;
REP0,
		'n'    => 1,
	),
	array(
		'name' => <<<'NAME1'
겹쳐 읽기 · 세 잣대를 주제별로
NAME1,
		're'   => <<<'RE1'
~\ \ \ \ page\(\
\ \ \ \ \ \ '<p\ class="eyebrow">마지막에\ 놓는\ 세\ 장</p>'\
\ \ \ \ \ \ \+\ '<h2>셋을\ 겹쳐\ 놓으면</h2>'~s
RE1,
		'rep'  => <<<'REP1'
    /* 주제별 글이 있으면 위에서 고른 갈래를 그것으로 바꿉니다.
       없으면 바로 위 원래 글이 그대로 나갑니다. */
    var __ct;
    __ct = comboSay(majorN === 0 ? 'm0' : (majorN === 1 ? 'm1' : 'm2'), majorN);
    if(__ct){ mSay = __ct; }
    __ct = comboSay(flow >= 4 ? 'up' : (flow <= -4 ? 'down' : 'flat'), 0);
    if(__ct){ fSay = __ct; }
    __ct = comboSay(revN === 0 ? 'r0' : (revN === 3 ? 'r3' : 'rs'), revN);
    if(__ct){ rSay = __ct; }

    page(
      '<p class="eyebrow">마지막에 놓는 세 장</p>'
      + '<h2>셋을 겹쳐 놓으면</h2>'
REP1,
		'n'    => 1,
	),
);


/* ------------------------------------------------------------------
 * 아래는 손대지 않으셔도 됩니다.
 * ---------------------------------------------------------------- */

add_action( 'admin_init', function () use ( $STELLA_C_EDITS ) {

	/* 앞서 보내드린 스니펫들이 모두 ?stella_patch= 였습니다. 익숙한 대로 치셔도
	   되도록 두 이름을 다 받습니다. 둘 다 없으면 아무 일도 하지 않습니다. */
	$raw = '';
	if ( isset( $_GET['stella_combo'] ) ) {
		$raw = $_GET['stella_combo'];
	} elseif ( isset( $_GET['stella_patch'] ) ) {
		$raw = $_GET['stella_patch'];
	}
	if ( '' === $raw || ! current_user_can( 'manage_options' ) ) {
		return;
	}

	$mode     = sanitize_key( wp_unslash( $raw ) );
	$page_id  = (int) STELLA_C_PAGE;
	$done_opt = 'stella_combo_' . STELLA_C_KEY . '_done';
	$back_opt = 'stella_combo_' . STELLA_C_KEY . '_backup';
	$log      = array();

	$say = function ( $lines, $ok = true ) {
		add_action( 'admin_notices', function () use ( $lines, $ok ) {
			printf(
				'<div class="notice notice-%s"><p><strong>스텔라 패치 · 말 맞추기</strong></p><pre style="white-space:pre-wrap;margin:0">%s</pre></div>',
				$ok ? 'success' : 'error',
				esc_html( implode( "\n", $lines ) )
			);
		} );
	};

	global $wpdb;
	$row = $wpdb->get_row( $wpdb->prepare(
		"SELECT ID, post_content FROM {$wpdb->posts} WHERE ID = %d", $page_id ) );
	if ( ! $row ) {
		$say( array( "페이지 {$page_id} 을(를) 찾지 못했습니다." ), false );
		return;
	}
	$content = $row->post_content;

	/* ---- 되돌리기 ---- */
	if ( 'undo' === $mode ) {
		$backup = get_option( $back_opt );
		if ( ! is_string( $backup ) || '' === $backup ) {
			$say( array( '되돌릴 내용이 없습니다.' ), false );
			return;
		}
		$wpdb->update( $wpdb->posts, array(
			'post_content'      => $backup,
			'post_modified'     => current_time( 'mysql' ),
			'post_modified_gmt' => current_time( 'mysql', 1 ),
		), array( 'ID' => $page_id ) );
		clean_post_cache( $page_id );
		delete_option( $done_opt );
		$say( array( '고치기 전 내용으로 되돌렸습니다.',
			'되돌린 크기 : ' . number_format( strlen( $backup ) ) . ' 바이트',
			'호스팅 → 성능 → 캐시 비우기 를 눌러주세요.' ) );
		return;
	}

	$dry = ( 'go' !== $mode );

	if ( get_option( $done_opt ) ) {
		$say( array( '이 패치는 이미 돌렸습니다 (' . get_option( $done_opt ) . ').',
			'다시 돌리려면 먼저 ?stella_combo=undo 로 되돌리세요.' ) );
		return;
	}

	/* ---- 고칠 자리가 하나씩 다 있는지 먼저 셉니다 ---- */
	/* WPCode 편집기에 붙여넣으면 줄바꿈이 \r\n 으로 바뀝니다. 그러면 여러 줄에
	   걸친 찾기 문구가 페이지의 \n 과 안 맞아 하나도 못 찾습니다.
	   그래서 찾기 문구와 바꿀 글을 쓰기 직전에 \n 으로 되돌립니다. */
	$fixnl = function ( $t ) { return str_replace( array( "\r\n", "\r" ), "\n", $t ); };

	$bad = false;
	foreach ( $STELLA_C_EDITS as $e ) {
		$found = preg_match_all( $fixnl( $e['re'] ), $content );
		if ( false === $found ) {
			$log[] = '⚠ ' . $e['name'] . ' — 찾다가 오류가 났습니다';
			$bad   = true;
			continue;
		}
		$log[] = ( $found === $e['n'] ? '   ' : '⚠ ' ) . $e['name'] . ' : ' . $found . ' / ' . $e['n'];
		if ( $found !== $e['n'] ) { $bad = true; }
	}
	if ( $bad ) {
		$say( array_merge( $log, array( '',
			'고칠 자리가 예상과 다릅니다. 엔진이 그 사이에 바뀐 것 같습니다.',
			'아무것도 고치지 않았습니다. 이 화면을 그대로 알려주세요.' ) ), false );
		return;
	}

	/* ---- 실제로 바꿔봅니다 ---- */
	$updated = $content;
	foreach ( $STELLA_C_EDITS as $e ) {
		$rep     = $fixnl( $e['rep'] );
		$updated = preg_replace_callback( $fixnl( $e['re'] ), function ( $m ) use ( $rep ) {
			$out = $rep;
			for ( $i = count( $m ) - 1; $i >= 1; $i-- ) {
				$out = str_replace( '\\' . $i, $m[ $i ], $out );
			}
			return $out;
		}, $updated );
		if ( null === $updated ) {
			$say( array_merge( $log, array( '', $e['name'] . ' 에서 바꾸다가 실패했습니다.' ) ), false );
			return;
		}
	}

	$grew  = strlen( $updated ) - strlen( $content );
	$sha   = sha1( $updated );
	$log[] = '';
	$log[] = '늘어난 바이트 : ' . number_format( $grew ) . '  (예상 ' . number_format( STELLA_C_GROW ) . ')';
	$log[] = '바뀐 본문 지문 : ' . substr( $sha, 0, 12 ) . '  (예상 ' . substr( STELLA_C_SHA, 0, 12 ) . ')';

	if ( abs( $grew - STELLA_C_GROW ) > 64 ) {
		$say( array_merge( $log, array( '',
			'늘어난 크기가 예상과 다릅니다. 뭔가 잘못된 것 같아 멈췄습니다.' ) ), false );
		return;
	}
	if ( $sha === STELLA_C_SHA ) {
		$log[] = '→ 제가 미리 걸어보고 확인한 것과 한 글자도 다르지 않습니다.';
	} else {
		$log[] = '→ 지문이 다릅니다. 크기는 맞으니 대개 줄바꿈 차이지만, 확인이 필요합니다.';
	}

	if ( $dry ) {
		$say( array_merge( $log, array( '',
			'※ 미리보기였습니다. 아무것도 고치지 않았습니다.',
			'진짜로 고치려면 ?stella_combo=go 로 다시 여세요.' ) ) );
		return;
	}

	update_option( $back_opt, $content, false );
	$ok = $wpdb->update( $wpdb->posts, array(
		'post_content'      => $updated,
		'post_modified'     => current_time( 'mysql' ),
		'post_modified_gmt' => current_time( 'mysql', 1 ),
	), array( 'ID' => $page_id ) );
	if ( false === $ok ) {
		$say( array_merge( $log, array( '저장 실패 : ' . $wpdb->last_error ) ), false );
		return;
	}
	clean_post_cache( $page_id );
	update_option( $done_opt, current_time( 'mysql' ), false );

	$say( array_merge( $log, array( '',
		'다 됐습니다.',
		'1) WPCode 에서 이 스니펫을 지우세요. (꼭)',
		'2) 호스팅 → 성능 → 캐시 비우기',
		'3) 시크릿 창에서 연성의 신 책을 한 권 뽑아 확인',
		'',
		'되돌리려면 : /wp-admin/?stella_combo=undo' ) ) );
} );
