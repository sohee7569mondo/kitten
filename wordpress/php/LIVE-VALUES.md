# 사이트에 실제로 들어가 있는 값

`wordpress/php/stella-signup.php` 는 **오래된 사본입니다.** 값이 사이트와
여러 군데 어긋나 있으니 값을 판단할 때 이 파일을 믿지 마세요.

확인하는 법 — 관리자로 로그인한 채 아래를 엽니다.

    https://stellasaju.com/?stella_check=1

## 2026-08-31 확인한 값

| 이름 | 저장소 사본 | **실제 사이트** |
|---|---|---|
| `STELLA_TRIAL` | true | **false** (실결제) |
| `STELLA_ORB_KRW` | — | **990** |
| `STELLA_PRICE_BASE` | 5 | **1** (풀이 한 편 990원) |
| `STELLA_PRICE_FULL` | 50 | **25** (인생길잡이 24,750원) |
| `STELLA_PRICE_ONELINE` | 5 | **0** |
| `STELLA_PRICE_DEEP` | 5 | **0** |
| `STELLA_WELCOME_ORBS` | 5 | **1** |
| `STELLA_EVENT_ORBS` | 15 | **1** |

가입하면 구슬 **2개**를 받습니다 (축하 1 + 오픈 기념 1).

## 한 줄 질문과 심화 질문이 0인 것은 의도입니다

실수가 아닙니다. 파는 단위를 「풀이 한 편」 하나로 두고, 질문은 값을
따로 매기지 않고 안에 전부 넣어 해설을 두껍게 하려는 것입니다.
0 을 보고 버그로 오해하지 마세요.

## 인생길잡이가 0원으로 보이는 것도 값 문제가 아닙니다

「STELLA - 결제 상태 점검」(스니펫 266) 107번째 줄과
「STELLA - 포트원 백엔드」(스니펫 265) 669번째 줄이
`stella_po_bundle_krw('guide')` 를 값으로 찍는데,
그 함수는 예전 「묶음 특별가」 장치이고 지금은 **일부러 0 을 돌려줍니다**
(스니펫 265 의 64번째 줄에 그렇게 적혀 있습니다).

진짜 값은 `stella_price() × STELLA_ORB_KRW` 로 셈합니다.
25구슬 × 990 = **24,750원** 이 맞습니다.

점검창 두 줄은 아직 안 고쳤습니다. 고칠 때 이 문서도 같이 지워주세요.

## 결제창을 보려면

구슬이 남아 있으면 풀이 한 편(1구슬)은 그냥 열려서 결제창이 안 뜹니다.
잔액보다 비싼 것을 골라야 뜹니다 — **나의 인생길잡이(25구슬)** 를 쓰세요.

## 스크립트 안에 앰퍼샌드를 쓰지 마세요

워드프레스의 `wptexturize` 가 페이지 본문 `<script>` 안의 맨 앰퍼샌드를
`&#038;` 로 바꿉니다. `&&` 하나가 페이지의 스크립트 덩어리를 통째로
죽입니다. 페이지 137 과 160 이 이것 때문에 백지가 됐습니다.
`if` 를 겹쳐 쓰거나 삼항 연산자를 쓰고, 문자로 필요하면 `\x26` 을 쓰세요.
페이지 160 은 `patch160` 스니펫이 `wptexturize` 를 떼어 막아두고 있습니다.

## 스니펫에 긴 줄을 넣지 마세요 (2026-08-31에 비싸게 배움)

주제별 글 패치를 보내면서 압축한 글 뭉치를 `define()` 한 줄에 담았습니다.
크기는 21KB로 작았지만 **그 한 줄이 12,576글자**였습니다.

그 스니펫을 저장한 순간부터 **WPCode 가 어떤 PHP 스니펫도 불러오지 않았습니다.**
플러그인은 활성이고 스니펫 목록에는 전부 「켜짐」으로 보이는데 실제로는
아무것도 실행되지 않는 상태였습니다. 그래서 이런 일이 한꺼번에 일어났습니다.

- /wp-json/stella/v1/me 가 rest_no_route (404)
- 사이트 앞면이 계속 로그아웃으로 보임 (메뉴가 /me 에 물어보기 때문)
- 가입해도 정보가 저장되지 않음
- 저장된 생년월일 메모가 안 뜸
- 구슬이 세어지지 않아 풀이가 무한정 열림
- ?stella_patch= 와 ?stella_check= 주소가 전부 홈으로

원인을 찾는 데 몇 시간이 걸렸습니다. 쿠키·캐시·로그인·플러그인을 차례로
의심했지만 전부 아니었고, 그 스니펫 하나를 끄자 즉시 전부 돌아왔습니다.

**규칙 · 스니펫의 어떤 줄도 200글자를 넘기지 마세요.**
긴 값은 100글자씩 쪼개 이어붙입니다.

    function stella_xxx_payload() {
        $p  = '';
        $p .= '첫 100글자';
        $p .= '다음 100글자';
        return $p;
    }

**그리고 스니펫을 새로 저장한 뒤에는 반드시 이 주소를 확인하세요.**

    https://stellasaju.com/wp-json/stella/v1/me

`{"ok":false,...}` 처럼 나오면 정상이고, `rest_no_route` 가 나오면
방금 저장한 스니펫이 WPCode 를 막은 것이니 즉시 꺼야 합니다.

## 성별 값 (2026-09-01 확인)

문 페이지의 고르는 칸 —

    <option value="">고르기</option>
    <option value="F">여성</option>
    <option value="M">남성</option>

usermeta `stella_sex` 에도 `F` / `M` 으로 그대로 저장됩니다. 회원 열 명을 확인한
결과 전부 F 아니면 M 이었습니다. 글을 성별로 가를 때는 이 두 글자를 보면 됩니다.

전자책(160)에 「상대」가 164번, 「그 사람」이 111번 나옵니다. 「남편」 8번,
「아내」 13번은 이미 쓰이고 있습니다. 연성의 신에서 손님 성별에 따라
남자친구·남편 / 여자친구·아내로 부르기로 했습니다 (사이트 주인의 요청).

## 전자책의 꼭지 42개 (2026-09-01, ?stella_sex=1 로 뽑음)

직성의 신 — 올해의 시기 흐름 · 그래서 언제 움직이고 언제 기다리는지 ·
            나에게 맞는 일의 결 · **돈이 들어오고 나가는 자리**(아직)
연성의 신 — 올해 인연의 흐름 · 언제 말하고 언제 기다리는지 ·
            나와 맞는 사람의 결 · 관계에서 되풀이되는 자리
강성의 신 — 올해 조심할 계절 · 무엇부터 챙기시면 되는지 · 타고난 체질의 결 ·
            가족 안에서 나의 자리 · 누구를 짊어지고 누구에게 기대는지
성좌의 신 — 달자리 · 태어나던 밤의 달 · 어떤 결로 사는 사람인지 ·
            별과 여덟 글자가 겹치는 자리 · 두 언어를 한 문장으로
아르카나 — 켈틱 크로스 열 장 외 여섯
공통     — 여덟 글자 · 기운의 저울 · 대운 · 달별 흐름 등 열여섯. 계산 결과라
            주제와 상관없이 같습니다.
