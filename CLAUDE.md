# 스텔라사주 (stellasaju.com) — 벼리가 먼저 읽을 것

이소희(소희 님)의 한국어 사주·타로·별자리 사이트. WordPress.com Atomic.
저는 이 저장소에서 **WPCode 조각(PHP 스니펫)** 을 만들어 드립니다.
소희 님이 그것을 붙여넣고 주소로 실행해 살아 있는 쪽을 고칩니다.

---

## ★★ 가장 중요한 것 — 제 사본은 낡았습니다

저는 **살아 있는 사이트를 볼 수 없습니다.** `stellasaju.com` 은 프록시
정책으로 막혀 있어 curl·WebFetch 가 다 403 입니다. WordPress.com MCP 로
쪽 정보(id · slug · modified)는 읽히지만 내용은 1.4MB 라 대화로 못 받습니다.

2026-09-13 에 직접 세어본 차이 —

    reading-book    살아 있는 것 1,427,388  ·  제 사본과 +312,292
    reading-start   살아 있는 것    58,456  ·  제 사본과  +36,215
    door-love       살아 있는 것    60,932  ·  제 사본과  +17,871

그래서 `wordpress/pages/*.html` 을 **믿고 앵커를 잡으면 못 찾습니다.**
2026-09-13 하루에 네 번 그랬습니다 (OPEN 셋째 · DOORLOVE 세 자리 ·
WEDSPLIT 여섯째). 소희 님께 「또 같은 일 반복하는 느낌」을 드린 까닭입니다.

## ★★ 그래서 앵커 규칙 — 반드시 지킵니다

1. **한 줄만 씁니다.** 두 줄을 붙여 찾으면 줄바꿈·들여쓰기 한 칸 차이로
   놓칩니다. 여러 줄이 꼭 필요하면 그 까닭을 조각 머리말에 적습니다.
2. **앞 들여쓰기를 떼고, 공백을 흡수하는 정규식을 씁니다.**
   `'  var topicKey = topic;'` → `/var\s+topicKey\s*=\s*topic\s*;/u`
3. **찾은 줄은 그대로 두고 그 뒤(또는 앞)에 끼웁니다.** 통째로 바꾸지 않으면
   뒤에 오는 글을 건드리지 않습니다.
4. **같은 이름이 여러 번 선언되는지 셉니다.** `LENS_TOPIC` 은 책에 **두 번**
   있습니다 — 앞은 직성의 신(`if(slug!=='door-career') return`), 뒤가 연성의
   신(`door-love`). 몇 번째를 쓸지 정하고 `preg_match_all` 로 그 자리만 바꿉니다.
5. **못 찾으면 멈추지 말고 그 자리 글을 찍어 줍니다.** 그러면 왕복이 한 번에
   끝납니다. 꼭 필요한 자리가 아니면 `soft` 로 두어 나머지는 넣습니다.
6. **「이미 넣었나」 표시는 그 조각만 만드는 것으로 봅니다.** `결혼운_미혼`
   같은 낱말은 다른 조각도 넣습니다 — WEDSPLIT 이 그래서 한 번도 안 들어갔습니다.
   블록 안에서만 보려면 중괄호를 세어 그 표만 떠서 봅니다.
7. **조각을 만들기 전에 앵커를 확인할 수 있습니다.** 아래 「이미 있는 도구」.

## ★ 앵커를 아예 안 잡는 길이 있습니다 (PFIX-1 에서 씀)

쪽의 글을 고치지 않고 **통로에 체를 거는** 식으로 풀리는 일이 있습니다.
그러면 사본이 낡아도 「0군데」가 날 일이 없습니다.

    localStorage.setItem 을 감쌉니다   — 주문이 저장될 때 값을 보태기
    window.fetch 를 감쌉니다           — 서버로 갈 주문에 값을 보태기
    그 쪽에서만 (is_page) wp_head 로 얹습니다

궁합의 `partner` 가 주문에 안 담기던 것을 이렇게 고쳤습니다
(`patch160_pfix`). 되돌리기도 따로 없습니다 — 스니펫을 끄면 원래대로입니다.
칸 이름은 여러 가지로 찾아 둡니다(`pMonth` · `pMonthSel`) — 사본이 낡아
이름을 정확히 못 보기 때문입니다.

**쪽 글을 꼭 바꿔야 하는 일(원고 넣기)만 앵커를 잡습니다.**

## ★★ 책이 하얗게 나오면 — 자바스크립트가 멈춘 것입니다

그리는 도중에 탈이 나면 **화면에 아무 말도 안 나옵니다.** 그냥 빕니다.
소희 님은 「아무것도 안나와」라고만 하실 수 있습니다. 그때 —

    ?gwhy=1        진단 띠 (patch160_gwhy)
                   처음 난 탈 · 궁합을 실제로 돌려본 결과 ·
                   책 그릇에 몇 자 들었나

2026-09-13 에 이것으로 잡았습니다 — `topicKey is not defined` (줄 9500).

**WEDSPLIT 이 만든 `topicKey` 는 만든 덩어리 안에서만 있습니다.**
책에는 따로 떨어진 작은 덩어리(IIFE)가 여럿 있고, 거기서 `topicKey` 를
부르면 없는 이름이라 그 자리에서 멈춥니다. `patch160_tkfix` 가
`window.topicKey` 를 미리 하나 만들어 두어 고쳤습니다.

**조각이 새 이름을 만들 때는 그 이름을 쓰는 자리가 같은 울타리
안인지 반드시 셉니다.** 아니면 처음부터 `window.` 에 답니다.

## ★★ 되풀이되는 탈 — 정규식 안의 빗금 (두 번 물렸습니다)

책 안에 이런 줄이 둘 있습니다 —

    pages[ci].replace(/<p class="dvno dvch">[^<]+<\/p>/, …)   장번호
    pages[i].replace(/<div class="folio">[^<]*<\/div>/, …)    쪽번호

여기서 **역빗금이 벗겨지면** 정규식이 `</` 에서 끝나버리고 뒤의 `p` 가
꼬리표로 읽혀 `Invalid regular expression flags` 가 납니다. 이것은
**글 읽기 자체가 안 되는** 탈이라 그 토막이 통째로 안 돌고,
**책이 한 글자도 안 그려집니다.**

    2026-09-11  SOS-2 로 고침
    2026-09-13  또 벗겨져 있었음 → SOS-3

**왜 벗겨지나 — 160쪽을 워드프레스 편집기로 열어 저장하면** 워드프레스가
홑 역빗금을 한 겹 벗겨냅니다. 소희 님께 **편집기로 저장하지 마시라고**
매번 말씀드립니다. 고칠 것은 늘 조각으로.

SOS-2 는 `[^<]*` 별표만 찾아서 `[^<]+` 더하기가 되어 있으면 못 찾습니다.
**SOS-3 은 모양을 안 가립니다** — `#(\[\^<\][*+])</([a-zA-Z][a-zA-Z0-9]*)>/#`

**조각을 만들 때마다 이것을 셉니다** — 넣는 글 안에 감싸지지 않은
`</` 가 정규식 안에 있는지. 보통 글에는 `[^<]*` 가 안 나오므로 이 모양만
보면 됩니다.

## ★★★ 쪽을 코드로 저장할 때는 wp_slash 로 감쌉니다 (2026-09-16)

`wp_update_post()` 와 `wp_insert_post()` 는 글을 저장하며 **역빗금을
한 겹 벗겨냅니다** (안에서 `wp_unslash` 를 부릅니다). 그래서 넘길 때
`wp_slash()` 로 감싸야 본디 글자가 그대로 들어갑니다.

    wp_update_post( array( 'ID' => $pid, 'post_content' => wp_slash( $new ) ) );

안 감쌌더니 스타 사주 쪽(406)의 역빗금 **다섯 개가 통째로** 사라졌습니다.

    v.replace(/\+/g, ' ')   →   v.replace(/+/g, ' ')

`/+/` 는 말이 안 되는 정규식이라 **그 script 덩어리가 통째로 안 돕니다.**
검색칸도 이름 단추도 아무것도 안 나왔습니다. 화면에는 오류 한 줄도
안 나옵니다 — 그냥 안 됩니다.

★ 이것은 「편집기로 저장하면 역빗금이 벗겨진다」는 **같은 탈의 다른 얼굴**
  입니다. 그 탈을 CLAUDE.md 에 두 번이나 적어두고선, 제가 코드로
  저장하면서 똑같은 짓을 했습니다.

★ **저장한 뒤에는 반드시 다시 읽어 역빗금을 셉니다.**
      $before = substr_count( $page->post_content, chr(92) );
      ... 저장 ...
      clean_post_cache( $pid );
      $after = substr_count( get_post($pid)->post_content, chr(92) );
  수가 다르면 그 자리에서 알 수 있습니다. 화면을 열어보고서야
  아는 것과 하늘과 땅 차이입니다.

## ★★ 쪽 내용을 **직접 읽을 수 있습니다** (2026-09-16)

「제 사본은 낡았습니다」가 더는 어쩔 수 없는 일이 아닙니다.

    wpcom-mcp-content-authoring · pages.get
      { id: 406, context: 'edit', include_fields: ['content'] }

230KB 라 대화로는 안 들어오지만 **파일로 떨어집니다.** 그것을 저장소에
두고 거기서 고치면 앵커를 짐작할 일이 없습니다.

    wordpress/pages/star-saju.live.html   살아 있는 쪽 그대로
    wordpress/pages/star-saju.new.html    고친 것
    wordpress/tools/build_starpage.py     둘 사이를 잇는 도구

★ 옛 사본(celeb.html)은 살아 있는 쪽보다 **13만 자** 뒤처져 있었습니다.
  그동안 앵커를 못 잡고 헤맨 까닭이 이것입니다.

## ★★ 조각 크기는 36KB 를 넘기지 않습니다 (2026-09-16)

52KB 짜리 조각은 붙여넣어도 **아예 안 켜졌습니다.** 주소를 열어도
「관리자만 볼 수 있습니다」조차 안 나왔습니다. 삼재 때 77KB 가
끊겼던 것과 같은 일입니다. 켜져 있던 patch_celebgo 가 36KB 이니
그 사이 어딘가가 한계입니다.

★ 크면 **쪼갭니다.** 쪼갤 때는 —
    · 순서를 안 가리게 (서로 다른 자리를 건드리게)
    · 두 번 눌러도 안전하게 — **자물쇠를 답니다**
      (post_meta 에 「다 넣었음」 표를 남기고, 있으면 물러납니다)
      ★ 원래 줄을 두고 둘레에 보태는 자리는 넣은 뒤에도 찾을 글이
        남아 있어서, 다시 누르면 같은 글이 겹쳐 들어갑니다

## ★★★ 큰 글에 정규식을 쓰기 전에 **가장 긴 줄을 셉니다** (504 를 냈습니다)

`patch_star6` 판a 가 **504 Gateway Time-out** 을 냈습니다. 서버가 멈췄습니다.

    '#[^' . chr(10) . ']*cd\.style\.paddingTop[^' . chr(10) . ']*#u'

쪽 406 안에 **한 줄이 105,126자**입니다 (STARS 배열 — 연예인 539명이
한 줄). PCRE 는 시작 자리마다 `[^\n]*` 를 줄 끝까지 먹고 되짚습니다.
10만자 줄에서 10만² = 1억 번을 넘습니다.

★ **제 사본 시험에서는 안 걸렸습니다.** CLI 의 `pcre.backtrack_limit`
  에 먼저 걸려 **조용히 빠져나갑니다** — 오류도 안 납니다. 그래서
  「잘 돌았다」고 보고했습니다. 웹 서버는 한계가 더 커서 진짜로 돕니다.

    조각을 만들기 전에 늘 이것부터 :
        python3 -c "import io; s=io.open(F,encoding='utf-8').read();
          ls=sorted(s.split(chr(10)),key=len,reverse=True);
          print([len(x) for x in ls[:5]])"

★ 고침 — **정규식을 아예 안 씁니다.** `strpos` · `strrpos` · `substr`
  만으로 같은 일을 합니다. 265KB 에서 **1밀리초**입니다.

      줄 하나를 지우려면 —
          $a = strpos( $s, '찾을말' );
          $ls = strrpos( $s, chr(10), $a - strlen($s) );   /* 앞 줄바꿈 */
          $le = strpos( $s, chr(10), $a );                 /* 뒤 줄바꿈 */
      strrpos 의 **음수 offset** 이 앞 줄바꿈을 한 번에 짚습니다.
      substr 로 앞을 떼어 훑지 않습니다.

★ `wp_cache_flush()` 도 뺍니다 — Atomic 에서 무겁습니다.
  `clean_post_cache( $pid )` 하나면 됩니다.

★ 504 가 났다고 **저장이 안 된 것이 아닙니다.** 이번에는 저장을
  마친 뒤에 멈췄습니다. 그래서 조각은 열리자마자 **지금 쪽 상태를
  먼저 찍습니다** — 「덮개 위 여백 100px · 재서 미는 줄 없음」.
  짐작하지 말고 숫자를 보게 합니다.

## ★★ 스텔라 랩 · 카카오 공유가 안 되던 까닭 (2026-09-17)

소희 님 「이건 아예 공유가 안됨」 → 화면 사진 한 장이 답을 줬습니다 —

    sharer.kakao.com/picker/link?app_key=0051c873...
    요청 실패 — 잘못된 요청으로 인증에 실패하였습니다

**코드 문제가 아닙니다.** SDK 도 실렸고 init 도 됐고 sendDefault 도
불렸고 카카오 공유창까지 떴습니다. 카카오가 **앱 키를 거절**한 것입니다.

    랩 검사 일곱 개   0051c873...   ← 거절당함
    유앤미           7806d048...   ← 잘 됨

랩 일곱(breakup·cafe·camping·grocery·money·night-snack·tokyo)이 **전부
같은 키**를 씁니다. 도쿄만의 탈이 아니었습니다.

★ **키만 유앤미 것으로 바꿔 시험했더니 카톡이 왔습니다.**
  → 코드는 멀쩡, **스텔라 랩 앱 설정**이 문제로 확정.
  짐작을 쌓지 말고 **한 줄만 바꿔 가려내는 시험**을 만드는 것이 빠릅니다.

★ 소희 님이 걱정하신 것 — 「키를 바꾸면 유앤미로 가는 것 아니냐」
  아닙니다. **앱 키는 카톡 카드 맨 아래 이름표만 정합니다.**
  링크는 코드의 shareUrl 이 정합니다.

★ 카카오 「사이트 도메인」에는 **경로를 넣지 않습니다** —
  `https://stellasaju.com` (O) · `https://stellasaju.com/lab/` (X)
  www 있는 것과 없는 것을 **다른 주소로 봅니다.** 둘 다 넣습니다.

### ★★★ 그림이 안 오던 까닭 — **너무 컸습니다** (2026-09-17 · 해결)

카톡 카드가 **글씨만** 왔습니다. 유앤미도 같았습니다.

짐작하지 말고 미디어를 직접 읽었습니다 —

    media.get id=979 → media_details
      width 1376 · height 768 · filesize 1,020,549  (1MB)

**1MB 짜리 32장.** 화면이 느린 것도, 카카오가 못 가져간 것도 같은
까닭이었습니다. 소희 님이 먼저 짚으셨습니다 —
「실제 테스트 할떄도 그림 뜨는데 시간이 오래 걸려」

★ **워드프레스가 이미 작은 판을 CDN 에 만들어 두었습니다.**
  `media_details.sizes` 에 그 주소 모양이 그대로 있습니다 —
      large 1024x572 · medium_large 768x429 · medium 300x167
  제가 만들 것이 아니라 **주소만 바꿔 쓰면 됩니다.**

      https://i0.wp.com/<주소에서 http:// 뗀 것>?w=1024

  `?ssl=1` 은 빼도 https 로 옵니다 — 그래야 **앰퍼샌드 0** 을 지킵니다.

    도쿄 : 문항 w=800 · 결과 w=1024 로 바꾸니 **카톡에 그림이 떴습니다.**

★★ 제가 「`i0.wp.com` 은 등록 안 된 도메인이라 역효과」라고 적었던 것은
   **틀렸습니다.** 카카오의 도메인 등록은 **SDK 를 실행하는 페이지**에
   대한 것이고, `imageUrl` 은 아무 데서나 가져옵니다.
   ☞ 짐작으로 규칙을 적어두면 다음 세션의 제가 그것을 믿고 헤맵니다.

★ 카카오는 한 번 실패한 그림을 **캐시**합니다. 주소가 바뀌면 (크기를
  붙이면 주소가 달라집니다) 바로 다시 가져갑니다.

★ 유앤미 ⑤번의 「카카오 서버에 올린다」는 길은 **안 써도 됩니다** —
  그림을 작게 하는 것으로 풀립니다.

## ★★ 랩 검사의 그림 이름이 미디어와 달랐습니다 (2026-09-17)

소희 님 「도쿄 링크 좋은데 안에 그림이 안들어감」

    조각이 찾던 것   tokyo-q1-e.jpg
    실제 올라간 것   Q1_E_Friends_planning_Tokyo_trip.jpeg

이름도 확장자(.jpg → .jpeg)도 달라 **32장이 하나도 안 나왔습니다.**

★ **미디어를 짐작하지 않고 직접 읽습니다** —
      wpcom-mcp-content-authoring · media.list
        { search: 'tokyo', per_page: 60,
          include_fields: ['id','title','source_url'] }
  `slug` 는 없는 칸입니다. id·date·status·type·mime_type·source_url·
  alt_text·media_details·author·post·title·caption 만 됩니다.

★ 선택지 **글과 그림 뜻을 하나씩 대조**합니다. 두 군데가 어긋났습니다 —
    q8  이름은 F/T 인데 문항의 축은 **P/J** 였습니다
    q2  이름의 S/N 이 그림 뜻과 뒤바뀌어 있었습니다
  이름을 믿지 말고 **글을 믿습니다.**

★ 워드프레스는 긴 파일 이름을 줄이며 **줄임표(…)** 를 넣습니다.
  주소에 쓸 때는 `%E2%80%A6` 로 인코딩합니다. 32장 중 10장이 그랬습니다.

★ 랩 조각은 `init` 에서 HTML 을 뿌리고 `exit` 하므로 **`wp_head` 로
  밖에서 얹을 수 없습니다.** 고치려면 그 조각을 고쳐야 합니다.

## ★★★ 확인하시라고 드리는 주소에 **가짜 값을 지어 넣지 않습니다** (2026-09-17)

유앤미 카톡 그림을 고치며 소희 님께 이런 주소를 드렸습니다 —

    …/uandme/card/91-lover-37-23-24-<이름>-v3f8a2c.png?w=800

`3f8a2c` 는 **제가 예시로 지어낸 가짜 md5** 였습니다. 진짜는
`e812a4` 입니다. 소희 님은 **없는 주소**를 여셨고 당연히 404 ·
「remote data could not be fetched」가 났습니다.

★ 그리고 저는 그 결과를 보고 **「CDN 이 원본을 못 가져온다」고 단정**해
  CDN 을 통째로 빼버렸습니다. 그 뒤로 세 번을 더 헛돌았습니다.
  나중에 살펴보기 화면이 진실을 보여줬습니다 —
      i0.wp.com/…/79-lover-30-12-21-ve812a4.png?ssl=1
      상태 200 · image/png · 583,500바이트   ← **잘 됩니다**

    확인해 보시라고 드리는 주소는 **살아 있는 값**이어야 합니다.
    모르면 「살펴보기 화면에 찍힌 주소를 그대로 여세요」라고 합니다.
    지어낸 값으로 만든 주소는 **실패가 보장**되고, 그 실패를
    사실로 받아들이면 길을 잘못 듭니다.

★ 같은 결의 잘못 — ④번 주석이 「카카오는 우리 서버 그림을 어떤
  방법으로도 가져가지 않았습니다」라고 적어두었는데, 저는 그것을 읽고도
  「작게 하면 되겠지」로 짐작해 /um-sq/ · CDN · /um-tall/ · .jpg 파일
  주소를 차례로 만들었습니다. **저장소에 이미 적힌 결론을 먼저 믿습니다.**

## ★★★ 「덮어써서 고치지 말고 원인을 알아보자」 (2026-09-17)

소희 님 「아니 안돼 그냥 덮어써서 고치지 말고 잘생각해서 원인을 알아보자」

저는 공유가 안 되는 까닭을 모른 채 **「이럴 수도 있다」를 넷 나열해
다 고쳤습니다.** 그건 짐작의 나열이지 원인이 아닙니다.

    까닭을 안 뒤에야 고칩니다. 모르면 **재는 것**을 먼저 만듭니다.
    화면 사진 한 장(카카오 오류창)이 나흘 걸릴 짐작을 한 번에 끝냈습니다.

★ 사실을 세는 버릇 — 「다른 앱」 단추가 **보였다**는 것 하나로
  「코드가 끝까지 돌았다 · 연결은 다 됐다」를 알 수 있었습니다.
  그런데 저는 그걸 보고도 「아무 반응 없음」으로 단정했습니다.

## ★★ 여백은 **더하지 말고 놓습니다** (2026-09-16 · 한없이 늘어났습니다)

소희 님 「마우스를 내렸다가 위로 올리면 한없이 공간이 생겨」

`patch_celebgo` 의 `topGap()` 이 이렇게 돼 있었습니다 —

    var now = parseFloat(getComputedStyle(best).paddingTop);
    best.style.paddingTop = Math.round(now + (need - docTop)) + 'px';   ★ 더합니다

`tick()` 이 되풀이 도는데, 쪽을 내리면 `r.top` 이 크게 음수가 되어
`need - docTop` 이 수백이 됩니다. 그것을 **이미 있는 여백에 또 더하니**
올릴 때마다 여백이 불어납니다. `GAPTRY > 6` 울타리도 스크롤 때문에
계속 다시 세어져 소용이 없었습니다.

    고침 : 잰 값을 더하지 말고 **그대로 놓습니다**
           best.style.paddingTop = Math.round(need) + 'px';
           그리고 한 번 놓으면 다시 안 잽니다 (깃발 하나)

★ 되풀이 도는 자리에서 `now + …` 를 쓰면 반드시 불어납니다.
  스타일을 **셈해서 놓는** 코드는 언제나 「지금 값」을 안 읽어야 합니다.

## ★★ 쪽을 고쳤으면 그 일을 하던 조각은 **끕니다**

스타 사주를 `patch_star1~5` 로 쪽 원본에 넣은 뒤에도 `patch_celebgo` 가
켜져 있어 같은 자리를 둘 다 그렸습니다. 소희 님 「리로드 할 때 두 가지로
나왔어 · 첫번째가 보이고 그 위에 두번째가 덮어 쓰는 방식인거 같아」 —
바로 그것입니다. **쪽에 넣는 순간 조각은 짐이 아니라 훼방입니다.**

    ?stella_over=1        어느 조각이 덮어쓰고 있나 (tool_over)
    ?stella_over=uandme   그 쪽에 실제로 실리는 조각과 크기 합

## ★★ 쪽 글을 찾을 때 빈칸에 기대지 않습니다 (2026-09-16)

살아 있는 쪽은 줄끝이 **섞여 있습니다** (`\r\n` 과 `\n` 이 함께).
그래서 여러 줄짜리 찾을 글이 안 맞습니다. 「17군데 가운데 4군데」처럼
**한 줄짜리만 맞는** 모양이 나오면 이것입니다.

    ① 먼저 줄끝을 \n 으로 맞춥니다 (쪽도, 찾을 글도)
       ★ 맞추는 일을 **찾을 글 목록이 만들어진 뒤**에 해야 합니다.
         조각 파일은 머리말 + 목록 + 본체 순서로 붙는데, 머리말에
         두면 아직 있지도 않은 것을 맞추게 됩니다 (제가 그랬습니다)
    ② 그래도 못 찾으면 **빈칸을 흘려 넘기며** 한 번 더 찾습니다
           preg_quote 한 뒤 빈칸 덩어리를 \s+ 로 바꿔
           한 군데만 맞을 때 씁니다
    ③ 덩어리를 자르는 표지말에는 **줄바꿈을 넣지 않습니다**

★ 「이미 들어갔나」는 **넣을 글이 있는지**로 가립니다. 「찾을 글이
  없다 = 이미 들어갔다」로 보면 거짓말을 합니다 — 못 찾은 것과
  이미 들어간 것은 다릅니다.
  그리고 찾을 글을 **먼저** 봅니다. 넣을 글이 찾을 글을 품는 자리가
  있어서 순서를 거꾸로 하면 거짓으로 걸립니다.

## ★ 진단 도구가 스스로 죽지 않게

GWHY-6 이 `GK.grade.name` 한 줄 때문에 통째로 안 그려졌습니다.
진단하려던 것이 진단을 막았습니다. 그래서 —

- **띠를 먼저 그려 놓고** 읽습니다 (「읽는 중…」)
- 읽는 부분은 통째로 `try` 로 감싸고, 멈추면 **거기까지 읽은 것 +
  멈춘 까닭**을 냅니다
- 서버가 발치에 한 줄 찍어 **조각이 켜져 있는지**를 따로 알려줍니다
- 띄우지(`position:fixed`) 않습니다 — 사이트 머리글에 눌립니다

## ★ 「오류가 있는 스니펫」이 뜰 때 — 대개 이미 들어 있는 것입니다

    Cannot redeclare function stella_wed_new()
            (previously declared in ... eval()'d code:5428)

이것은 조각이 잘못된 것이 **아닙니다.** 같은 이름의 함수가 이미 WPCode 에
있다는 뜻입니다 — **그 조각이 이미 저장돼 있고 켜져 있습니다.**
소희 님이 붙여만 놓고 **주소를 안 여신 것**입니다.

    새로 붙이지 마시고   →   주소만 엽니다
                             ?stella_wed=preview  →  =apply

2026-09-13 에 이것으로 한 번 돌았습니다. 조각을 드리기 전에 먼저
여쭙니다 — **「WPCode 목록에 이 이름이 이미 있나요?」**
또는 `?stella_snip=stella_wed` 로 찾아봅니다.

**그래서 조각을 여러 개 한 파일로 묶지 않습니다.** 하나라도 이미 있으면
묶음 전체가 저장이 안 됩니다.

## ★ 되돌리기(undo)는 위험할 수 있습니다

조각의 백업은 **그 조각을 처음 apply 한 순간의 쪽 전체**입니다. 그 뒤에 다른
조각을 여럿 넣었으면 undo 가 그것들까지 통째로 날립니다. 그래서 —

- undo 는 **두 단계**로 만듭니다 — 크기를 먼저 보여주고, 차이가 크면 멈춤
- apply 할 때 **백업을 지금 쪽으로 갱신**합니다 (GAP-7 처럼)

---

## 집 규칙 (어기면 손님 화면이 깨집니다)

- **페이지 `<script>` 안에 앰퍼샌드를 쓰지 않습니다.** `&&` 도 홑 `&` 도요.
  `if` 를 겹쳐 쓰거나 `String.fromCharCode(38)` 을 씁니다. PHP 참조 `&$x` 도
  피합니다 — 검사할 때 0 이 나와야 마음이 놓입니다.
- **JS 문자열은 홑따옴표**라 본문 인용은 「」 로 씁니다. `"` 도 피합니다.
- **PHP 안에 JS 를 담을 때는 nowdoc** — `<<<'TAG'` … `TAG;`
- **주석 안에서 별표와 빗금이 붙지 않게 합니다.** 2026-09-17 에
  머리말에 `**/um-sq/…**` 라고 썼더니 `*/` 가 생겨 **주석이 그 자리에서
  끝났습니다.** 뒤의 한글이 코드로 읽혀 php -l 이 「unexpected 바꿔」를
  냈습니다. 주소를 굵게 쓰지 말고 「」 로 감쌉니다.
      검사 : 여러줄 주석을 떠서 그 안에 `*/` 가 또 있는지 셉니다
- **WPCode 위치(Location)는 「어디서나 실행 / Run Everywhere」**
- **크롬 번역을 끄고 붙여넣어야 합니다.** 번역이 코드를 건드려 조각이 꺼진
  적이 있습니다. 조각 머리말에 매번 적습니다.

## ★ 진단 화면을 만들 때 (2026-09-13 에 걸린 것)

`template_redirect` 에서 워드프레스 쪽을 안 그리고 직접 HTML 을 뿌리고 `exit`
하면 **`wp_head` 가 아예 안 불립니다.** 그래서 다른 조각이 `wp_head` 에 얹은
스크립트가 그 화면에는 없습니다. 「★ ○○이 없습니다」로 잘못 나옵니다.

    </head> 앞에서  do_action( 'wp_head' );  를 손으로 부릅니다.

GCALC-2 · GTEXT-2 에서 그렇게 고쳤습니다.

## ★ 화면 모양이 이상할 때 — 눈으로 재지 말고 크로미움으로 잽니다

2026-09-13 에 가족운 책이 「폭이 이상해」 했습니다. 사진만 보고는 까닭을
못 찾습니다. **책의 진짜 CSS 를 떠서 실제로 그려 재면 한 번에 끝납니다.**

    # 1. 쪽에서 CSS 를 다 떠냅니다
    python3 -c "import io,re; s=io.open('wordpress/pages/reading-book.html',
      encoding='utf-8').read();
      io.open('book.css','w',encoding='utf-8').write(
        ''.join(re.findall(r'<style[^>]*>(.*?)</style>', s, re.S)))"

    # 2. 시험 쪽을 만듭니다 — 겉 구조를 똑같이 해야 합니다
    <div id=ssb><div class="book" id="bkBook" data-family="1"> … 우리 쪽들 … </div></div>
    ★ #bkBook 은 class="book" 도 갖고 있습니다. 이걸 빼면 CSS 가 안 먹어서
      엉뚱한 값이 나옵니다 (여백 0 으로 나와서 한참 헤맸습니다).

    # 3. 크로미움으로 재봅니다
    const {chromium}=require('playwright');
    chromium.launch({executablePath:'/opt/pw-browsers/chromium-1194/chrome-linux/chrome'})
    ★ 그냥 launch() 하면 「Executable doesn't exist」 가 납니다.
      경로를 손으로 넣어야 합니다. ls /opt/pw-browsers 로 판을 확인하세요.

    # 4. 쪽마다 글 시작 자리를 잽니다
    els.map(e => e.querySelector('h2,p').getBoundingClientRect().left)
    → 다 같으면 우리 HTML 은 죄가 없습니다.

## ★ 화면에서는 쪽 좌우 여백이 0 입니다

책 CSS 에 이렇게 돼 있습니다 — 손님이 내려가며 읽는 편이 낫다고 하셔서
2026-08-23 에 바꾼 것입니다.

    @media screen{
      #ssb .book{gap:0;}
      #ssb .page{ background:transparent; border:0; box-shadow:none;
                  min-height:0 !important; padding:46px 0 6px; }
      #ssb .folio{display:none;}
    }

낱장 모양(테두리·그림자·쪽번호)은 **인쇄할 때만** 살아납니다.
그리고 글이 시작하는 자리는 쪽이 아니라 **책 상자**가 정합니다 —
`.book{ margin:0 140px; padding:32px 20px 120px }` → 140 + 20 = 160px.

## ★★ 폭이 쪽마다 다르면 — 원인은 flex 의 auto margin 입니다

2026-09-14 소희 님 「중간에 폭이 달라짐」 · 「폭잡는것도 여러번 있던
일이라서 찾아보면 답이 있을거야」 — 있었습니다. 두 가지입니다.

**① 책의 폭은 `patch160_width`(WIDTH-7) 하나가 정합니다.**
   그 머리말에 「책의 모양 — 이 조각 하나가 정합니다」라고 적혀 있습니다.
   쪽 900px · 좌우 여백 78px · 글줄 744px.

**② 그런데 WIDTH-7 은 `width:auto` + `margin-left/right:auto` 로 놓습니다.**
   책(.book)은 `display:flex; flex-direction:column` 이라,
   **flex 안에서 좌우 margin 이 auto 면 stretch 가 꺼집니다.**
   그래서 글이 짧은 쪽은 글 길이만큼 줄어들고 가운데로 몰립니다.
   재 보니 239px ~ 780px 까지 제각각, 글 시작 자리 세 가지였습니다.

       고침 : auto margin 을 0 으로 눌러 stretch 를 살립니다

**③ 앞에 `#ssb` 를 꼭 붙입니다.** WIDTH-7 이 `#ssb .page` 로 걸어서,
   `[data-ny2026="1"] > .page` 처럼 쓰면 힘이 모자라 집니다.
   어떤 쪽은 내 규칙이, 어떤 쪽은 WIDTH-7 이 이기게 됩니다.

       가족운   #ssb .book[data-family="1"] > .page   ← 이깁니다
       전에 신년  [data-ny2026="1"] > .page             ← 집니다

★ 재는 도구가 이미 있습니다 — `?stella_wide=1` (tool_wide).
  #ssg 부터 위로 열두 겹까지 넓이 · max-width · 여백을 표로 띄웁니다.
  짐작으로 CSS 를 얹기 전에 이것부터 엽니다.
★ 인라인 스타일이 상대면 CSS 로는 못 이깁니다 — `patch160_fit`(FIT-2)가
  자바스크립트로 직접 밀어넣는 길을 써 두었습니다.

## ★ 책을 갈아끼우는 조각에는 폭 울타리를 칩니다

살아 있는 쪽에는 제 사본에 없는 CSS 가 얹혀 있을 수 있습니다. 그래서
우리가 갈아끼운 책에만 걸리는 못을 박아 둡니다.

    @media screen{
      #ssb .book[data-family="1"] > .page{
        padding-left:0 !important; padding-right:0 !important;
        margin-left:0 !important; margin-right:0 !important;
        max-width:none !important; width:auto !important; }
      #ssb .book[data-family="1"] > .page > *{ … 같은 못 … }
    }

★ `!important` 가 꼭 있어야 합니다. 없으면 훼방 규칙에 집니다
  (일부러 `padding-left:135px !important` 를 넣고 겨뤄서 확인했습니다).
★ 우리 깃발이 붙은 책에만 걸리므로 다른 책은 안 건드립니다.

## ★ 같은 자리를 노리는 조각이 둘이면 깃발을 같이 꽂습니다

`patch160_health`(HEALTH-3)와 `patch160_health4`는 둘 다 건강운 책을
갈아끼웁니다. 깃발이 `data-health` / `data-health4` 로 달라서 서로를
못 알아보고 덮어썼습니다.

**새 조각이 일을 마친 뒤 옛 조각의 깃발까지 같이 꽂습니다.** 그러면 옛
조각이 재시도할 때 「이미 됐구나」 하고 물러납니다. 소희 님이 옛 조각을
못 끄셔도 새 조각이 이깁니다.

그리고 조각은 **이름 붙은 PHP 함수를 쓰지 않습니다** — 전부 add_action 에
이름 없는 함수를 넘깁니다. 그래야 두 번 붙여도 「Cannot redeclare」가
안 납니다. 스크립트도 `if(window.StellaXxx){ return; }` 로 한 번만 돌게
막습니다.

## ★ 원고를 손으로 옮겨 적지 않습니다

`wordpress/tools/` 에 원고(.md) 를 조각으로 바꾸는 도구가 있습니다.

    build_family.py / emit_family.py     가족운
    build_health4.py / emit_health4.py   건강운 (### 안에 ###### 가 있는 두 층)

원고를 고치면 이 둘을 다시 돌려 조각을 새로 뽑습니다. 도구가 하는 일 —

    · ★ 나 ☞ 로 시작하는 문단은 **제 메모**라 책에 안 내보냅니다
      (HEALTH-1 때 메모가 책에 그대로 찍혔습니다)
    · 태그 속성을 홑따옴표로 씁니다 → JSON 이스케이프가 안 생겨
      **역빗금 0**. 편집기 저장에 역빗금이 벗겨지는 되풀이 탈을 피합니다
    · 규칙에 적은 갈래 제목이 원고에 **실제로 있는지** 만들 때마다 검사합니다
    · 제목에도 `{이름}` 을 넣습니다 (본문에만 넣어서 한 번 틀렸습니다)

## ★★ 조각마다 울타리를 칩니다 — 안 그러면 모든 쪽이 다 받습니다

2026-09-14 소희 님 「스니펫이 많아서 그거 읽느라 사주가 문제있다고
햇었는데」 — 맞는 말씀이었습니다. WPCode 위치가 「어디서나 실행」이라
`wp_head` 에 그냥 걸면 **홈 · 무료 쪽 · 문 쪽에 다 실립니다.**
2026·2027 책 두 권이 547KB 이라 손님이 어느 쪽을 열든 그것을 다
받고 있었습니다.

    add_action( 'wp_head', function () {
        if ( ! is_page( 'reading-book' ) ) { return; }
        ?> … <?php
    } );

★ 「어디서나 실행」은 그대로 둡니다 — 울타리는 조각 안에서 칩니다.
★ 슬러그는 짐작하지 않고 워드프레스에서 직접 읽습니다
  (`pages.list`). 틀리면 책이 아예 안 나옵니다.

    확인한 슬러그  reading-book(160) · door-love(141) · door-career(130)
                   door-health(169) · door-fortune(170) · door-astro(171)
                   door-tarot(172) · saju(59) · zodiac(82) · zodiac-year(92)
                   weekly-summary(84) · today(678) · lotto(79)
    홈             쪽 id 62 를 앞쪽으로 씁니다 → is_front_page()
    ★ lab 쪽은 아직 없습니다 (2026-09-14 기준)

울타리를 친 뒤 쪽마다 받는 양 (php 로 흉내 내어 잼) —

    홈             12,561 자
    reading-book  547,000 자   (책 두 권 — 이 쪽은 어쩔 수 없습니다)
    문 여섯         4,026 자
    사주풀이 · 무료 쪽들   0 자

## ★★ 신년운세 책 — 2026-09-14 에 통째로 다시 지었습니다

소희 님이 열 장을 **손님 한 분(인성 해 × 거두고 쌓는 십 년)에게 쓴 편지**로
다시 쓰시고, 갈래 칸을 장마다 채워 주셨습니다. 그것을 제자리에 붙였습니다.

    한 손님이 받는 2026 책   31,807자  →  약 11,000자

### 원고에 꼬리표를 답니다 — 제목 글이 바뀌어도 안 깨집니다

    ### 채우고, 다음을 준비하세요 [인성]      갈래 (세운 다섯)
    ### 거두고 쌓는 십 년 [거두고 쌓는 십 년]   대운 (십 년 다섯)
    ### [알맞은 자리]                        강도 셋
    # [비겁]  →  ## × 나를 세우는 십 년       스물다섯 조합
    # [이음]                                제목 없이 문장만 (다리)

★ 꼬리표는 **책에 안 나갑니다.** `[비겁]` 만 남기면 제목도 안 나갑니다.
★ `# [비겁]` 은 갈래만 열고 제목은 안 냅니다 — 무리 머리가 화면에
  찍히던 것을 이렇게 막았습니다.
★ `# [이음]` 은 갈래로 넘어가는 다리 한 줄. 머리가 있어야 앞 덩어리에
  글이 안 붙으므로, 머리는 두고 보이는 제목만 비웁니다.

### ★★ 본문 없는 머리는 **버려집니다** (절기 열둘이 통째로 날아갔습니다)

    # 입춘 · 2월 4일 ~ 3월 4일
    ### 지난 것을 정리하고 …        ← 바로 소제목이 오면

머리 덩어리에 본문이 하나도 없어 `always` 가 걸러집니다. 절기 이름과
날짜가 다 사라졌는데 **화면에는 아무 표시도 안 났습니다.**
소제목을 굵은 글씨 한 줄로 합쳐 살렸습니다.
**머리를 만들 때는 그 아래 한 줄이라도 본문이 있는지 셉니다.**

### 해를 글에 박지 않습니다 — 소희 님 「2026년이라는 표현은 빼세요」

    {연도} {다음해} {지난해} {간지} {올해오행}
    {이름} {올해이름} {대운이름} {대운십년}

★ 「올해」는 얼마든지 씁니다 — 그려질 때 그 해를 뜻합니다.
★ `python3 check_year.py 2026` 이 간지 예순 · 오행 다섯을 다 봅니다.
  0 이 나와야 다음 해에 그 원고를 그대로 씁니다.

### ★ 문단 안에서 줄을 쪼개지 않습니다

소희 님 「문단은 짧게 가지 말고 한 줄씩 갔으면」 — md2html 이 문단 안
줄바꿈을 `<br>` 로 바꾸고 있었습니다. 빈칸으로 둡니다.
인용(`> `)에서는 줄바꿈이 그대로 살아납니다.

### 도구 (wordpress/tools/)

    dump_split.py 2026 틀      소희 님이 채우실 칸만 (빈 틀)
    dump_split.py 2026 03      한 장의 갈래 글 전부
    blocks_newyear.py 2026 세운  갈래 기준으로 세로로 모은 원천 문장
    check_year.py 2026        해가 박힌 자리
    check_always.py 2026      모두에게 나가는 글이 얼마나 되나

### ★ 조각에 판 시각이 찍힙니다

소희 님 「이거 수정안햇어」 — 파일은 고쳐져 있는데 화면에 옛 판이
돌고 있었습니다. 이제 머리말과 화면 소스에 만든 시각이 찍힙니다.

    <!-- stella 2026년 운세 · 판 2026-09-14 16:25 -->

책 쪽에서 Ctrl+U 로 찾으면 지금 돌고 있는 판을 알 수 있습니다.

### ★★ 카드 세 장 — 신년운세에는 원래 없었습니다 (덮인 것이 아닙니다)

소희 님 「비상… 신년운세에 카드 3장 고른거에 대한 내용이 안들어가」

저는 우리 조각이 덮어서 사라진 줄 알고 「덮기 전에 원본 카드 쪽을
살려 붙이는」 길을 만들었습니다. 그런데 관리자 띠가 **「살린 카드 쪽
0쪽」** 이라고 말해 주었습니다 — **처음부터 없었습니다.**
카드 세 꼭지는 `if(slug==='door-tarot')` 안에만 있습니다.

    ★ 짐작으로 고치지 말고, 숫자를 찍어 주는 띠부터 만듭니다.
      「0쪽」 한 마디가 사흘 걸릴 헛수고를 막았습니다.

**카드 표를 다시 적지 않습니다** — 책이 이미 내놓고 있습니다.

    window.StellaTarot      일흔여덟 장 (메이저 22 + 마이너 56)
                            카드마다 now · over · then 세 풀이
    localStorage stella_cards  reading-start 가 담습니다 (번호 셋)
    자리 읽는 법             I 지금 서 있는 자리 · II 넘어야 할 것 · III 그 너머

카드 장은 **⑨ 뒤 · ⑩(건네는 말) 앞**에 놓습니다. 배웅이 마지막이라야
책이 제대로 닫힙니다. 그래서 ⑩ 은 「제 11 장」이 됩니다.
카드를 안 뽑으셨으면 그 장을 아예 안 만듭니다.

### ★★ 진단은 두 겹으로 — 자바스크립트가 안 돌면 자바스크립트 띠도 안 뜹니다

소희 님이 「표지도 없고 내용도 없어」를 세 번 말씀하시는 동안 저는
까닭을 몰랐습니다. `go()` 가 `catch(e){}` 로 멈춘 까닭을 삼키고
있었습니다. 그래서 두 겹으로 만들었습니다 —

    서버(PHP)가 찍는 노란 줄   울타리 **밖** · wp_footer
                              조각이 켜져 있기만 하면 무조건 보입니다
                              지금 쪽 슬러그와 판 시각을 같이 찍습니다
    자바스크립트 초록 띠        단계마다 말합니다 (책 그릇 · 주문 · 주제 ·
                              원본 책 · 못 지음 · 멈춘 까닭 · 그린 결과)

둘 다 `current_user_can('manage_options')` 로 막아 손님에게는 안 보입니다.

    노란 줄도 초록 띠도 없다  → 조각이 안 켜졌거나 옛 판이 돕니다
    노란 줄만 있다           → 자바스크립트가 멈춘 것
    둘 다 있다               → 초록 띠가 멈춘 자리를 말해 줍니다

## ★★ 책 폭 — 자리를 갈라야 안 싸웁니다 (같은 날 두 번 고치다 되돌림)

    책 상자(.book)  폭을 정합니다   max-width:900 · margin auto · padding 20
    쪽(.page)       늘어나게 둡니다 margin 0 · max-width none

같은 자리(.page)에 둘을 다 걸었더니 하나를 고치면 다른 하나가
되살아났습니다 — auto margin 은 flex 아이템에서만 stretch 를 끄므로
상자에 걸면 쪽은 그대로 늘어납니다. **네 책이 같은 값을 씁니다.**
훼방 규칙(padding-left:135px !important)을 얹고 겨뤄서 확인했습니다.

★ 인용(.nysay)은 배경 없이 왼쪽 바만. padding-left 에 !important 를
  주고도 울타리에 지므로 울타리 안에서 한 번 더 박습니다.

## 검증 절차 (조각을 드리기 전에 다 합니다)

    php -l              앞에 <?php 를 붙여 문법 검사
    grep -c '&'         앰퍼샌드 0 이어야 합니다
    ★ 정규식을 쓴다면   고칠 쪽의 **가장 긴 줄**을 먼저 셉니다 (위 꼭지)
                        10만자 줄이 있으면 정규식을 쓰지 않습니다
    ★ 그림을 쓴다면     media_details 로 **크기와 바이트**를 먼저 봅니다.
                        1MB 가 넘으면 화면도 느리고 카카오도 못 가져갑니다.
                        i0.wp.com/<주소>?w=1024 로 줄여 씁니다
    ★ 역빗금은 grep -c '\\\\' 로 세면 **거짓말을 합니다** (2026-09-16)
      그것은 역빗금 **두 개**를 찾습니다. 홑 역빗금은 grep -o 로 셉니다.
      그리고 PHP 문자열의 "\n" 은 괜찮습니다 — 위험한 것은
      **<script> 안**의 홑 역빗금입니다. 그것만 따로 셉니다 :
          python3 -c "import io,re; s=io.open(F,encoding='utf-8').read();
            print(''.join(re.findall(r'<script>([\\s\\S]*?)</script>',s)).count(chr(92)))"
      patch_starfind 의 replace(/\\s+/g,'') 두 군데가 이 검사에 걸렸습니다.
    사본에 실제로 적용   str_replace 를 흉내내 돌려봅니다
    node --check        결과에서 <script> 를 다 뽑아 문법 검사
    로직 시험           node 로 함수를 실제로 돌려봅니다 (pickedSide 처럼)
    화면 시험           **jsdom 을 깔 수 있습니다** (npm i jsdom). 살아 있는
                        화면 글을 그대로 붙여 놓고 조각을 실제로 돌려봅니다.
                        runScripts:'dangerously' 로 script 를 붙이고,
                        DOMContentLoaded 뒤를 보려면 setTimeout 으로 기다립니다.
    겹치는 줄 검사       주제별 원고는 줄이 겹치면 안 됩니다

## 이미 있는 도구 — 짐작하지 말고 이것으로 봅니다

    ?stella_audit=1            표마다 어느 주제가 들어 있나 (tool_audit)
    ?stella_check=1            쪽 크기 · 궁합 재료 · WPCode 조각 목록 (tool_check)
    ?stella_find911=찾을말     어느 쪽에 있나 (tool_find)
    ?stella_snip=찾을말        어느 스니펫에 있나 (tool_snip)
    ?stella_peek=...           책의 한 토막 그대로 보기 (tool_peek)
    ?stella_over=1             덮어쓰는 조각 · 쪽마다 실리는 양 (tool_over)
    ?stella_umon=1             유앤미 조각 아홉의 켜짐/꺼짐 (tool_umon)

**조각을 만들기 전에 `?stella_find911=` 이나 `?stella_snip=` 으로 앵커가
살아 있는지 소희 님께 여쭤보는 편이 빠릅니다.** 만들고 놓치는 것보다요.

---

## 소희 님께 (계속 유효한 지시)

- 「보통 네가 글을 잘써… 나한테 안 물어보고 전부 글을 써도 무방하다고 봐」
  → **카피는 묻지 말고 씁니다.** 쓰고 나서 보여드립니다.
- 「길이 맞추지마… 인위적으로 짧게 하면 내용이 바뀔 수 있어」
- 「또 물어봐야 해? 빨리 오픈하고 싶은데」 → **확인 질문을 줄이고 진행합니다.**
  갈림길이 결과를 크게 바꿀 때만 여쭙습니다.
- 비개발자입니다. 디자인 전공, 50대, 캐나다 거주. 말은 쉬운 우리말로,
  코드 낱말은 꼭 필요할 때만.

## 문(가디언)과 주제

    door-love     벼리   연애운 · 결혼운(미혼/기혼) · 재회운 · 이별운 ·
                         이혼운 · 짝사랑 · 궁합
    door-career   마루   취업운 · 직장운 · 이직운 · 퇴사운 · 사업운 · 재물운
    door-health   아람   건강운 · 체질 · 가족운 · 부모운 · 자녀운 · 형제운
    door-fortune  미르   신년운세 · 대운 · 올해 총운 · 전환기 · 삼재 · 평생운
    door-astro           별자리
    door-tarot           타로

★ 가디언 이름을 틀리지 않습니다 (2026-09-14 에 제가 틀렸습니다)
    door-career 는 **마루**입니다. 제가 CLAUDE.md 에 「아라」로 적어 두어
    한참 그렇게 알고 있었습니다. 아라는 **별자리(door-astro)** 쪽입니다.
    그리고 홈(home-v16.html)에는 직업·재물 줄이 **「일을 짓는 가디언 · 미르」**
    로 일곱 군데 잘못 적혀 있습니다 — 미르는 door-fortune(신년운세)입니다.
    ☞ 책에 가디언 이름을 쓸 때는 이 표를 먼저 봅니다. 2027 ⑥~⑩ 의
      「미르의 마지막 한마디」는 신년운세라 맞습니다.

## ★ 무료 쪽 — 주소와 이름이 어긋난 것이 있습니다

    /zodiac-year/     슬러그는 year 인데 그 쪽 제목은 「이번주 띠별운세」
                      셈도 그 주 월요일 일간으로 합니다 (홈 스크립트 주석에도
                      「띠별운세 · 별자리운세와 같은 값」이라 적혀 있습니다)
                      홈 배너만 「올해 띠별 운세」로 잘못 적혀 있었습니다
                      → patch160_freelabel 이 고칩니다. 주소는 그대로 둡니다
                        (바꾸면 이미 퍼진 링크가 끊깁니다)

    무료 다섯 장  /today/ · /saju/ · /weekly-summary/ · /zodiac/ · /zodiac-year/
    /lotto/       2026-09-14 소희 님 「로또 없애자」 — 홈 줄에서 숨깁니다
                  (patch160_freeimg 의 HIDE 표). 쪽은 그대로 있습니다.
                  지우지 않고 숨기는 까닭 : 가로로 미는 줄(rail)의 스크립트가
                  배너 수를 세고 있을 수 있어 들어내면 화살표 셈이 틀어집니다.

    무료 배너 사진  STELLASAJU_free_<자리>.jpg  (미디어, 2026/09)
                  zodiac-year(토끼) · zodiac(별자리) · saju(오솔길) ·
                  weekly(호수) · today(들꽃)
                  ★ 토끼만 784x1168 이라 2:3 이 아닙니다 — 위아래 조금 잘립니다
                  STELLASAJU_turn.jpg(사슴) 은 전환기/평생운 배너용으로 올려둠

★ 홈 배너 글씨는 **앵커를 안 잡고** 고칩니다 — `a.free-card` 를 href 로 찾아
  `.free-kicker` · `.free-sub` 만 갈아 끼웁니다. 사본이 낡아도 맞습니다.

## ★ 홈 아래 줄 — 가디언 소개 → 주제 배너 (patch160_askmore)

2026-09-14 소희 님 「여기 가디언소개 말고 2026운세 2027운세 건강운
가족운 인생총운 삼재 배너를 넣자고」

    바뀌기 전  건강·가족 / 흐름·시기 / 점성술 / 스텔라타로  (door-card 넷)
    바뀐 뒤    2026년 운세 · 2027년 운세 · 건강운 · 가족운 ·
               인생총운 · 삼재                              (ask-card 여섯)

★ 점성술 · 스텔라타로는 홈에서 빠집니다 — 머리글 차림표에는 그대로 있습니다
  (별을 읽는 신 · 아르카나). 홈에 다시 두려면 뒤에 두 장 더 붙이면 됩니다.
★ 배너 딱지는 「인생총운」인데 문 단추는 「평생운」입니다 — 주소는
  `?topic=평생운` 이라 제대로 잡힙니다. 손님이 두 낱말을 보게 되므로
  언젠가 하나로 맞추는 편이 낫습니다.
★ 홈의 줄 스크립트는 **처음에 카드 수를 세어 두고** 그만큼 점을 만듭니다.
  넷→여섯으로 늘면 점이 넷에 머무릅니다. 그래서 askmore 가 점을 다시
  그리고 켜는 셈도 다시 답니다 (우리 것이 나중에 걸려 옛 셈을 덮습니다).

**door-health · door-fortune 은 단추는 있는데 `DOOR_TOPICS` 에 없습니다.**
그래서 손님이 골라도 주제가 책에 전해지지 않고 문 단위 글 하나가 나옵니다.
door-love 에서 2026-09-13 에 고친 것과 같은 상태입니다.

## 주제별 표 (reading-book 안)

    AXIS_WARM_TOPIC   저울 다섯의 긴 설명        AXIS_TITLE_TOPIC  저울 제목
    AXIS_SIDE_TOPIC   저울 양쪽 이름             COMBO_TOPIC       조합 풀이
    CLOSE_TOPIC       편지                       LAST_TOPIC        배웅 한 줄
    LENS_TOPIC        1부 렌즈 (★ 두 번 선언)    KEEPTOPIC         맺음말 세 가지
    AGAINST_TOPIC     거스르는 자리              OPEN_TOPIC        여는 말
    INTRO_MID         장 안내 가운데 한 줄       PICKS_TOPIC       질문 답↔저울 대조
    DOOR_TOPICS       문마다 고를 수 있는 주제

찾을 때는 `topicKey` 를 먼저 보고 없으면 `topic`, 그것도 없으면 문 단위로
물러납니다. `topicKey` 는 결혼운을 미혼·기혼으로 가르려고 WEDSPLIT 이 만듭니다.

## 질문 답과 저울은 한 쌍입니다

`PICKS` 표가 손님 답을 **글자 그대로** 견줍니다. 질문 문장을 바꾸면 `PICKS` 의
a·b 도 같이 바꿔야 합니다. 안 그러면 저울 다섯이 전부 「모름」으로 떨어집니다.
`a` 는 A 쪽(안정·정답·혼자·실물·하나), `b` 는 B 쪽입니다.

## ★★ 유앤미 — 2026-09-15 에 카드와 셈을 통째로 다시 지었습니다

소희 님 「전부 무료 · SNS 에서 바이럴되서 스텔라에 들어오게 하기 위함」

### ★★ 유앤미가 통째로 비면 — 조각 ①②⑥ 가운데 하나가 꺼진 것입니다

2026-09-16 소희 님 「유앤미 페이지가 빈 화면」 → 「유앤미 스텔라 랩
비활성화된거 열었더니 유앤미 사이트 보임」. **조각 하나가 꺼져 있었습니다.**

유앤미 쪽(번호 760)에 든 글은 **딱 한 줄** `<div id="um"></div>` 뿐입니다.
화면은 조각 아홉이 `wp_head` 로 그립니다. 그래서 —

    ① 계산기      UANDME_TIERS 등 점수 셈        ★ 없으면 빈 화면
    ② 화면 밑감    UM_SET · UM_PIC · UM_CHARS ·   ★ 없으면 빈 화면
                  UM_CELEB (⑥이 스무 번 부름)
    ⑥ 화면 그리기  실제로 그림                    ★ 없으면 빈 화면
    ③④⑤⑦⑧⑨     쪽 만들기 · 카톡 · 에스엔에스 · 짧은 주소 · 이상형

**⑥은 ②의 이름들을 쓰는데, ②가 꺼지면 「없는 이름」에서 멈춥니다.
자바스크립트는 멈출 때 화면에 아무 말도 안 합니다 — 그냥 빕니다.**
머리글·바닥글만 남아서 쪽이 지워진 것처럼 보입니다.

★ **조각 이름이 저장소 파일 이름과 다릅니다.** 소희 님은 「유앤미
  스텔라 랩」 · 「⑥번 화면 그리기」처럼 붙이십니다. `?stella_check=1`
  의 켜진 목록을 `uandme_2_screen` 같은 이름으로 찾으면 못 찾습니다.
★ 그래서 **이름이 아니라 조각 속 표지말로** 찾습니다 — `tool_umon`
  (`?stella_umon=1`). 아홉을 표지말로 찾아 켜짐·꺼짐과 **번호**,
  그리고 그 조각 고치는 자리로 바로 가는 주소까지 찍습니다.
      ① UANDME_TIERS   ② um-chars.js   ③ stella_um3
      ④ stella_um_bg   ⑤ um_kimg       ⑥ uandme-screens.js
      ⑦ jetpack_open_graph_tags        ⑧ REQUEST_URI 대입문
      ⑨ UM_IDEAL_ON
★ **한 조각만 여는 확인 주소가 빠릅니다** — `?stella_um2=1` 은 `init` 에
  걸려 쪽을 안 가리므로, 켜져 있으면 무조건 그 화면이 나옵니다.
  **홈으로 튕기면 그 조각이 꺼진 것입니다.** 한 번 여는 것으로 끝납니다.
★ 쪽이 아니라 조각을 의심합니다. MCP 로 쪽을 봐도 `publish` 라 멀쩡해
  보입니다 — 쪽에는 원래 한 줄밖에 없기 때문입니다.

### 관계마다 점수가 달라집니다 (그 전에는 거의 같았습니다)

    소희 님 「친구 궁합은 높은데 결혼궁합은 점수가 낮은 것도 있어?」
             「송은이 유재석같이 친구로서는 좋은데 이성으로서는?」

    재 보니 안 났습니다 — 400쌍에서 관계 사이 점수차 가운데값 4점.
    관계에 반응하는 것이 십성 하나(100점 중 7점)뿐이었습니다.

    고침 : MIX — 여섯 재료(간·지·오행·십성·별자리·띠)의 몫을 관계마다
           다르게. SPAN — 관계마다 모이는 자리가 달라지므로 제 폭으로 폄.
    뒤   : 가운데값 18점 · 10점 넘는 쌍 92% · 평균은 일곱 다 57~60 으로 고름

★ 관계 단추를 누르면 **다시 셈해야** 합니다. 전에는 이름만 바뀌고 점수는
  그대로였습니다. 점수가 같던 때는 티가 안 났지만 이제 화면이 거짓말을 합니다.
★ 결과 화면에 일곱 관계를 한꺼번에 보여줍니다 (relMapHtml).

### 낱말표 — 카드에 나가는 글은 전부 「점수 칸」이 맡습니다

    wordpress/drafts/유앤미-낱말표.md   ← **원본**. 여기만 고칩니다
    tools/uandme_words.py              원고를 읽습니다 + 규칙 검사
    tools/emit_uandme_words.py         조각 둘에 함께 넣습니다
    tools/dump_words.py                원고를 다시 뽑습니다

    이름 칸 스무 개 (5점 단위)   요약 · 이모지 · 별명 · 본문 두 문장
                                 → 카드는 **여기만** 씁니다. 관계를 안 가립니다.
    해설 칸 관계7 x 큰칸5        훅 한 줄 · 긴 글 세 문장
                                 → 결과 화면과 카톡 설명글에서만

★★ 본문을 해설 칸(다섯)에 두었더니 아래 여덟 칸이 **전부 같은 글**이
   나왔습니다. 「운석과 지구 같은 사이」에 「웃음이 안 끊깁니다」가 붙었습니다.
   판 스무 장을 한 화면에 모아 보고서야 찾았습니다.
★ 이름 칸에 관계를 가리는 낱말(연애·밀당·콩깍지·부부·남매·회사)을 쓰면
  검사가 잡습니다. 「콩깍지 만료 임박」이 형제자매에 나왔습니다.
★ 한자·이모지·따옴표를 본문에 쓰지 않습니다 — 카드 글꼴에 없어 네모(□).

### 카드 — 소희 님이 그린 판 위에 글자만 얹습니다

GD 로는 그 결을 못 그립니다. 판은 그림으로 받고 서버는 글자만 얹습니다.

    ?stella_um_bg=auto                 이름에 칸 숫자가 든 그림을 다 찾아 넣기
    ?stella_um_bg=95=a.jpg;90=b.jpg    여러 칸을 한 줄에 (이름만 써도 찾습니다)
    ?stella_um_bg=1                    판 시각 · 조각 수 · 만든 카드 수 · 칸마다
    ?stella_um_bg=clear                만들어 둔 카드 그림 지우기
    ?stella_um_pos=10|0.501,0.668,0.70 말풍선 자리 (가운데x, 가운데y, 안쪽 너비)
    ?stella_um_sheet=1                 스무 칸을 한 장에 모아 보기

★ 자리는 **비율**로 잡습니다. 어떤 크기로 올려도 맞습니다.
★ 판마다 말풍선 자리가 다릅니다. 소희 님은 판만 주시고, 자리는 제가 잽니다.
★ 관리자가 볼 때는 캐시를 안 물리고 파일도 늘 덮어씁니다. 안 그러면
  판을 바꿔도 처음 만든 그림이 계속 나옵니다.
★ 카드 파일 이름에 판·자리의 md5 를 답니다. 안 그러면 옛 그림이 남습니다.

### 글꼴은 역할마다 다릅니다 (한 벌로 쓰면 촌스럽습니다)

    점수         포핀스 엑스트라볼드   숫자만 쓰는 자리라 한글이 없어도 됩니다
    별명         고딕A1 블랙
    이름·요약·점  고딕A1 볼드
    본문·발치     고운돋움

★★ **Jua 에는 가운뎃점(·)이 없습니다.** 전부 Jua 로 찍던 「유앤미 · 연인 궁합」이
   「유앤미 □ 연인 궁합」으로 나가고 있었습니다. 열한 벌을 실제로 그려 견주다
   눈으로 찾았습니다.
★ 흰 테두리는 GD 에 없는 기능입니다 — 같은 글자를 흰색으로 여덟 둘레에 찍고
  그 위에 본색을 얹습니다.

### ★★ 조각을 고칠 때는 **떼어내 진짜로 그려 봅니다**

`if ( $font ) { if ( $BG ) {` 부터 줄머리 `\n\t} else {\n` 까지 떼어
글꼴을 물려 돌리면 실제 카드가 나옵니다. 이것으로 찾은 탈들 —

    · 본문 첫 줄이 말풍선 위로 삐져나옴 (글자는 밑선에 찍힙니다)
    · 점수와 별명이 겹침 (밑선에서 여백만 빼면 글자 높이만큼 파고듭니다)
    · 글씨 크기를 말풍선 자리에 비례시켰더니 점수가 괴물처럼 커짐
    · 토막을 뗄 때 안쪽 else 에 걸려 잘림 → 줄머리로 찾습니다

### 카톡

    글머리   이모지 + 별명          그림에는 이모지를 못 넣습니다
    설명글   두 사람 · 관계 · 점수 · 훅
    그림     서버가 그린 카드. 크기는 판에서 읽어 window.UM_CARD_WH 로 알려줍니다

★ 판이 세로(2:3)라 카톡 채팅방에서는 위아래가 잘립니다. 가로 한 장이 더
  필요합니다 — 아직 없습니다 (2026-09-15 기준).

## 궁합은 별도 구조입니다

책에 `if(topic==='궁합')` 갈래가 따로 있고, 상대 생년월일 입력도 door-love 에
이미 있습니다(`pAlias` `pRel` `pSex` `pYear` `pMonth` `pDay` `pHour` `pMin`).
없는 것은 `window.StellaGunghapCalc` 와 `window.StellaGunghapText` 두 조각뿐입니다.
셈법과 저울은 `wordpress/notes/궁합-계산기-설계.md` 와 `궁합-재료저울-제안.md` 에.

신년운세 · 가족운 · 체질 · 삼재 · 인생총운도 같이 별도 구조로 갑니다.

---

## 일하는 결

- 조각 하나가 두 쪽을 고쳐야 하면 **한 자리라도 못 찾으면 아무것도 안 바꿉니다.**
  반쪽만 고쳐지면 저울이 어긋납니다.
- preview 는 **무엇이 지워지는지 그대로 찍어** 보여드립니다. 소희 님이 눈으로
  보고 apply 하십니다.
- 커밋 메시지에 **왜 그렇게 했는지**와 **무엇을 검사했는지**를 적습니다.
  다음 세션의 제가 읽습니다.
- 브랜치는 `claude/patch160-lv30-wpcode-189ko0`. 모든 작업을 여기에 올립니다.
